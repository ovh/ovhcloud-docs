---
title: "Configuring DNS Forwarder in NSX"
description: "You can use a DNS forwarder in NSX that centralises all DNS queries and redirects them to external DNS servers, which reduces network traffic. You can attach th"
zendesk_id: 15900827269139
zendesk_hash: 691e18fbcf1ccca9
content_hash: 790063c5d453c56d
updated: 2023-09-29
---

# Configuring DNS Forwarder in NSX

You can use a DNS forwarder in NSX that centralises all DNS queries and redirects them to external DNS servers, which reduces network traffic. You can attach the DNS forwarder to the north-south gateway (ovh-T0-gw) or the east-west gateway (ovh-T1-gw), depending on your needs.

**Discover how to configure a DNS forwarder.**

## Requirements

- Being an administrative contact of your [Hosted Private Cloud infrastructure](https://us.ovhcloud.com/hosted-private-cloud/vmware/) to receive login credentials.
- A user account with access to the [OVHcloud Control Panel](https://us.ovhcloud.com/manager/#/hub).
- You need to have **NSX** deployed with a segment configured in your NSX configuration. Use our guide on [Segment Management in NSX](/guides/hosted-private-cloud/vmware/nsx-segment-management) for more information.

## Instructions

We will create a DNS redirector and attach it to the north-south gateway (ovh-t0-gw), it will be usable on all segments of NSX.

### DNS forwarder configuration

Through the NSX interface go to the `Networking` tab and click on `DNS` on the left in the **IP Management** section. Then click `ADD DNS SERVICE`.

![DNS configuration - NSX configure DNS forwarder - step 1](/images/hosted-private-cloud/vmware/nsx-configure-dns-forwarder/configdns-01.jpg)

Enter this information :

- **Name** : Your service name, such as `dns-forwarder`.
- **Tier-0/Tier-1 Gateway** : Your North-South gateway `ovh-T0-gw`.
- **DNS Service IP** : a private IP address not used in your segments, such as `192.168.200.1`.

Next, click the `three vertical dots` below **Default DNS Zone**.

![DNS configuration - NSX configure DNS forwarder - step 2](/images/hosted-private-cloud/vmware/nsx-configure-dns-forwarder/configdns-02.jpg)

 

Click `Add New Default Zone`.

![DNS configuration - NSX configure DNS forwarder - step 3](/images/hosted-private-cloud/vmware/nsx-configure-dns-forwarder/configdns-03.jpg)

Fill in these values :

- **Name** : The name of the zone, such as `cdns.ovh.net`.
- **DNS Servers** : OVHcloud DNS server `213.186.33.99`.

Then click `SAVE`.

![DNS configuration - NSX configure DNS forwarder - step 4](/images/hosted-private-cloud/vmware/nsx-configure-dns-forwarder/configdns-04.jpg)

 

Click `SAVE`.

![DNS configuration - NSX configure DNS forwarder - step 5](/images/hosted-private-cloud/vmware/nsx-configure-dns-forwarder/configdns-05.jpg)

Your DNS forwarder is active and usable on all your overlay or VLAN segments connected to your north-south gateway (ovh-t0-gw).

![DNS configuration - NSX configure DNS forwarder - step 6](/images/hosted-private-cloud/vmware/nsx-configure-dns-forwarder/configdns-06.jpg)

### Editing the DHCP server to use a DNS forwarder

If you have configured DHCP servers in NSX as described in our [DHCP Configuration](/guides/hosted-private-cloud/vmware/nsx-configure-dhcp-onsegment) guide, you have the option to modify the configuration to use the DNS forwarder in your DHCP configurations.

We will see how to modify the DNS server on the DHCP configuration of one of your segments.

Still from the `Networking` tab, click `Segments` on the left in the **Connectivity** section.

Then click on the `three vertical dots` next to your segment and choose `Edit` from the menu.

![DNS configuration - NSX configure DNS forwarder - step 7](/images/hosted-private-cloud/vmware/nsx-configure-dns-forwarder/configdns-07.jpg)

 

Click `EDIT DHCP CONFIG`.

![DNS configuration - NSX configure DNS forwarder - step 8](/images/hosted-private-cloud/vmware/nsx-configure-dns-forwarder/configdns-08.jpg)

 

Click the `X` to the right of the existing DNS in the **DNS Servers** option to remove it from the configuration.

![DNS configuration - NSX configure DNS forwarder - step 9](/images/hosted-private-cloud/vmware/nsx-configure-dns-forwarder/configdns-09.jpg)

 

Add your DNS redirector IP address in **DNS Servers** as `192.168.200.1` and click `APPLY`.

![DNS configuration - NSX configure DNS forwarder - step 10](/images/hosted-private-cloud/vmware/nsx-configure-dns-forwarder/configdns-10.jpg)

 

Click `SAVE`.

![DNS configuration - NSX configure DNS forwarder - step 11](/images/hosted-private-cloud/vmware/nsx-configure-dns-forwarder/configdns-11.jpg)

 

Click `CLOSE EDITING`.

![DNS configuration - NSX configure DNS forwarder - step 12](/images/hosted-private-cloud/vmware/nsx-configure-dns-forwarder/configdns-12.jpg)

Your segment's DHCP settings now use the DNS forwarder you created earlier.

## Conclusion

Having read this guide, you should be able to configure a DNS forwarder.
