---
title: "How to Set Up Nutanix Objects"
description: "Nutanix Objects implements an Object Storage S3™ \\ compatible solution on your Nutanix cluster, using Prism Central. You can read the description on this link."
zendesk_id: 17631822493203
zendesk_hash: f7efcc1ce7788414
content_hash: 30afef2d9d8e0986
updated: 2025-01-23
---

# How to Set Up Nutanix Objects

Nutanix Objects implements an **Object Storage** S3™ \* compatible solution on your Nutanix cluster, using Prism Central. You can read the description on [this link](https://www.nutanix.com/content/dam/nutanix/resources/datasheets/ds-objects.pdf).

**This guide will show you how to set up Nutanix Objects on your cluster.**

## Requirements

- A Nutanix cluster in your OVHcloud account.
- Access to the [OVHcloud Control Panel](https://us.ovhcloud.com/manager/#/hub).
- You must be connected to the cluster via Prism Central.

## Instructions

From Prism Central, click your cluster in the **Cluster Quick Access** widget to connect to Prism Element.

![Set up Nutanix objects - step 1](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-01.png)

 

Click your cluster's name in the top left-hand corner next to the **X**.

![Set up Nutanix objects - step 2](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-02.png)

 

Verify the IP Address in the **ISCSI Data Services IP** section, then click `Save`.

![Set up Nutanix objects - step 3](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-03.png)

 

### Configuring VLANs in Prism Central

You need to create two additional VLANs in your cluster configuration and have these VLANs access Prism Central and Prism Element to configure **Nutanix Objects**. These two VLANs must be able to distribute IP addresses. IPAM will be enabled on both VLANs.

From Prism Central, go to the main menu and click on `Subnets` in the **Network & Security** category.

![Set up Nutanix objects - step 4](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-04.png)

 

Click `Create Subnet`.

![Set up Nutanix objects - step 5](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-05.png)

 

Check the `IP Address Management` box and fill in this information :

- **Name**: Type `object-storage`.
- **Type**: Select `VLAN`.
- **VLAN ID**: Type your VLAN number as `100`.
- **Virtual Switch**: Keep `vs0`.
- **Network IP Address / Prefix**: Write your subnet `192.168.100.0/24`.
- **Gateway IP Address**: Enter the address of your gateway `192.168.100.254`.

Then click `Add IP Pool`.

![Set up Nutanix objects - step 6](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-06.png)

 

Enter this information :

- **Start Address**: Start address of your scope as `192.168.100.10`.
- **End Address**: End address of your scope as `192.168.100.200`.

Then click the blue validation button `✓` on the right.

![Set up Nutanix objects - step 7](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-07.png)

 

Scroll down and enter the OVHcloud DNS address `213.186.33.99` in **Domain Name Servers** and click `Create`.

![Set up Nutanix objects - step 8](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-08.png)

 

Your first VLAN is created. We will create the second VLAN, click on `Create Subnet`.

![Set up Nutanix objects - step 9](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-09.png)

 

Check the `IP Address Management` box and fill in this information :

- **Name**: Type `public-storage`.
- **Type**: Select `VLAN`.
- **VLAN ID**: Type your VLAN number as `101`.
- **Virtual Switch**: Keep `vs0`.
- **Network IP Address / Prefix**: Write your subnet `192.168.101.0/24`.
- **Gateway IP Address**: Enter your gateway address `192.168.101.254`.

Then click `Add IP Pool`.

![Set up Nutanix objects - step 10](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-10.png)

 

Enter this information :

- **Start Address**: Start address of your scope as `192.168.101.10`.
- **End Address**: End address of your scope as `192.168.101.200`.

Next, click the blue validation button `✓` on the right.

![Set up Nutanix objects - step 11](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-11.png)

 

Scroll down and enter the OVHcloud DNS address `213.186.33.99` in **Domain Name Servers** and click `Create`.

![Set up Nutanix objects - step 12](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-12.png)

 

Your two VLANs are created, make sure they can connect to Prism Element and Prism Central through your gateway. You can use this guide to replace the default gateway and use your VLANs: [OVHgateway replacement](/guides/hosted-private-cloud/nutanix-on-ovhcloud/software-gateway-replacement).

![Set up Nutanix objects - step 13](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-13.png)

 

### Enabling Nutanix objects in Prism Central

Go to the main menu, scroll to the **Services** section, and click `Objects`.

![Set up Nutanix objects - step 14](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-14.png)

 

Click `Enable`.

![Set up Nutanix objects - step 15](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-15.png)

 

Click `Enable` to validate the activation of **Nutanix Objects**.

![Set up Nutanix objects - step 16](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-16.png)

 

The service checks your system compatibility.

![Set up Nutanix objects - step 17](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-17.png)

 

Click `Next`.

![Set up Nutanix objects - step 18](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-18.png)

 

Click `Create Object Store`.

![Set up Nutanix objects - step 19](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-19.png)

 

Fill in this information :

- **Object Store Name**: the name of your object storage, such as `nutanix-objects`.
- **Domain**: The domain name that will be used to access your storage, such as `mydomain.com`.
- **Cluster**: The cluster that must have Nutanix Objects enabled.
- **Worker Nodes**: The number of nodes that your Object storage will use, select the number `1`.

Then click `Next`.

![Set up Nutanix objects - step 20](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-20.png)

 

Select the VLAN `object-storage` in **Storage Network** and enter these two addresses `192.168.100.1,192.168.100.2` in **Object Store Storage Network static IPs (2 IPs required)**. These addresses must not be within the scope of the VLAN DHCP.

Then click `Next`.

![Set up Nutanix objects - step 21](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-21.png)

 

Select the `public-storage` VLAN in **Storage Network** and enter this range of four addresses `192.168.101.1-192.168.100.4` in **Public Network static IPs**. These addresses should not be within the scope of the VLAN DHCP.

Next, click `Save & Continue`.

![Set up Nutanix objects - step 22](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-22.png)

 

A connection check is performed between the VLANs, Prism Element, and Prism Central.

![Set up Nutanix objects - step 23](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-23.png)

 

When validation is complete, click `Create Object Store`.

![Set up Nutanix objects - step 24](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-24.png)

 

Wait twenty minutes for your Object Storage to be active and accessible through the address **192.168.101.1** that is located in your **public-storage** VLAN.

![Set up Nutanix objects - step 25](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-25.png)

 

### Creating an Access Key to your storage

To be able to use Object storage, it is necessary to create associated **Access Keys**.

Go to the `Access Keys` tab.

![Set up Nutanix objects - step 26](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-26.png)

 

Click `+ Add People`.

![Set up Nutanix objects - step 27](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-27.png)

 

Check `Add people not in a directory service`, type `user@mydomain.com` in **Email Address**, and click `Next`.

![Set up Nutanix objects - step 28](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-28.png)

 

Click `Generate Keys`.

![Set up Nutanix objects - step 29](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-29.png)

 

Click `Download Keys`.

![Set up Nutanix objects - step 30](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-30.png)

**Note:** Your access credentials are only displayed once in your web browser. Please note down this information (Access Key and Secret Key) before closing this window.

![Set up Nutanix objects - step 31](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-31.png)

 

Your user is created with its login details. You can regenerate Access Keys and create a new login.

![Set up Nutanix objects - step 32](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-32.png)

 

### Validation and functional testing

To confirm that our Object storage is working properly, we will use the AWS awscli command-line tool and create a bucket. You can use [this guide](/guides/storage-and-backup/object-storage/getting-started) for more information.

The awscli tool is available on Windows, Linux, and macOS.

Use the following command for help:

```
aws s3 help
```

After installing awscli, we will create two configuration files on a computer that has access to the public-storage VLAN and use the aws command to create a bucket.

Create a `.aws` subfolder in your `%USERPROFILE%` folder on Windows, or `$HOME` on Linux and macOS.

Add the `credentials` file and fill it in with this data:

```
[default]
aws_access_key_id=your-access-key
aws_secret_access_key=secret-access key
```

Then add a second file named `config` with this information :

```
[default]
region=region = us-east-1
```

Then use this command to create a bucket.

```
aws --endpoint-url https://192.168.101.1 s3 mb s3://my-bucket --region=us-east-1 --no-verify-ssl
```

The bucket is created with a warning message about the SSL certificate. In a production environment, it is necessary to use an SSL certificate.

Now that the bucket is created we will use a browser to connect to the web interface provided by Nutanix to check the existence of our bucket.

Using a web browser, connect to this URL `https://192.168.101.1/objectsbrowser` which corresponds to your **Nutanix Objects** access point, and enter your credentials (access\_key and secret\_key). Then click on `Login`.

![Set up Nutanix objects - step 33](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-33.png)

 

You can see the bucket you created in the command line. You can create and delete buckets or objects in your web interface.

![Set up Nutanix objects - step 34](/images/hosted-private-cloud/nutanix-on-ovhcloud/set-up-nutanix-objects/setupobjects-34.png)

 

## Go further
Browse all the [Nutanix guides](/guides/hosted-private-cloud/nutanix-on-ovhcloud/landing-page-nutanix-on-ovhcloud).

Browse the rest of the [OVHcloud US documentation](https://docs.us.ovhcloud.com/) for guides on our other products and services.

Learn more about [Nutanix on OVHcloud](https://us.ovhcloud.com/hosted-private-cloud/nutanix/) on the OVHcloud US website.

\*S3 is a trademark filed by Amazon Technologies, Inc. OVHcloud's service is not sponsored by, endorsed by, or otherwise affiliated with Amazon Technologies, Inc.

