---
title: "Public VCF as-a-Service - Creating a new virtual machine"
description: "To create your virtual machine, click on the name of the data center where you plan to deploy it."
zendesk_id: 28512564557587
zendesk_hash: c2aa29c714c4e927
content_hash: ce1b1226d3cd6a8a
updated: 2026-07-23
---

import { Tab, Tabs } from '@rspress/core/theme';

# Public VCF as-a-Service - Creating a new virtual machine

## Requirements

- Public VCF as-a-Service
- An ISO of an operating system

 

## Instructions

### Creating a virtual machine

To create your virtual machine, click on the `name of the data center` where you plan to deploy it.

:::info

You can also create a VM by clicking `Applications` > `Virtual Machines` > `NEW VM`.

:::

![Home page - VM creation](/images/hosted-private-cloud/public-vcf-aas/vm-creation/home-page.png)

On the next page, you should see the **Virtual Machines** menu. Click `NEW VM`.

![Data centers virtual machines](/images/hosted-private-cloud/public-vcf-aas/vm-creation/data-centers-virtual-machines.png)

<Tabs>

<Tab label="From template">

What is a **template**? A template is a preconfigured virtual machine that is used to create new ones. The amount of memory, vCPU, network cards (NICs) and storage space are already defined and cannot be changed during deployment. Don't worry, you can change it once the deployment process is complete. This is a basic OVA deployment. Back to creating your virtual machine. Fill in the required fields, then make sure to select the `Type` option as `From Template`. You will then see a dropdown menu of the different models available.

![Create VM from template - step 1](/images/hosted-private-cloud/public-vcf-aas/vm-creation/create-vm-from-template-01.png)

The templates available below are part of the pre-provisioned OVHcloud catalog, and can be used in a way that suits your needs. We provide a bunch of different Linux distributions with and without applications already installed, we also imported some models from the [Bitnami catalog](https://bitnami.com/stacks/virtual-machine).

In the `Custom Properties` section, you can customize various properties of your virtual machine, such as a specific user, the domain name, its IP address, and many others.

</Tab>

<Tab label="From scratch">

To create a VM from scratch, fill in the required fields, however for the option `Type` please select `New`. In the `Boot image` section, you will find preconfigured ISOs provided to you.

![Create VM from scratch - step 1](/images/hosted-private-cloud/public-vcf-aas/vm-creation/create-vm-from-scratch-01.png)

The `Compute` section allows you to configure the desired amount of vCPU and memory for this virtual machine.

</Tab>

<Tab label="From clone">

What is a **Clone**? A Clone is a copy of a virtual machine. In the case of a VM, the disk settings correspond with the machine used. You need to launch the copy from the VM: Click on `Applications` > `Virtual Machines` > `Actions` > `Copy`.

**WARNING:** A vApp must always be added on VDC. Follow the dropdown menu for the various settings available, just like creating a VM.

![Create VM clone](/images/hosted-private-cloud/public-vcf-aas/vm-creation/create-vm-clone.png)

</Tab>

</Tabs>

For the NIC section, you can choose the type of NIC you want. The default type (VMXNET3) is often a good choice if you don't have any special requirements.

Regarding network selection, you will find all available networks in your organization. In our example, we have enabled a DHCP service on this network and configured it with subnet `192.168.1.1/24`.

![Create VM from template - step 2](/images/hosted-private-cloud/public-vcf-aas/vm-creation/create-vm-from-template-02.png)

The IP Mode drop-down list provides different IP assignment modes:

- None
- DHCP: Use the DHCP server (your VM must manage the DHCP even if you select it).
- Static - IP Pool: Selects an IP from the network IP pool.
- Static Manual: You must set the IP manually.

**WARNING**: This IP mode only allows you to organize your VCD inventory, but you will still need to configure the IP (or DHCP) inside the VM.

In the `Storage` section, you will only see the VDC Default.

You can also add virtual disks in this section if you need more than one.

To conclude, please review all information provided to ensure accuracy. Next, click `OK` to launch the virtual machine deployment. Once the deployment is complete, you will see the new VM listed in the selected virtual data center.

If you have used the **From scratch** method, you just need to turn on your server and launch the installation of your OS.

 

## Go further
Browse the rest of the [OVHcloud US documentation](https://docs.us.ovhcloud.com/) for guides on our other products and services.

Learn more about [Hosted Private Cloud](https://us.ovhcloud.com/hosted-private-cloud/) on the OVHcloud US website.

If you need training or technical assistance to implement our solutions, contact your sales representative or click on [this link](https://us.ovhcloud.com/support/professional-services/) to get a quote and ask our Professional Services experts for a custom analysis of your project.

