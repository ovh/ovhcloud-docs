---
title: "OpenSearch™ - Getting started"
description: "Learn how to configure a Cloud Analytics with OpenSearch™ service in the OVHcloud Control Panel."
zendesk_id: 21890623420947
zendesk_hash: a19858e339de1907
content_hash: 64a27c576cc68303
updated: 2025-07-21
---

# OpenSearch™ - Getting started

**Learn how to configure a Cloud Analytics with OpenSearch™ service in the OVHcloud Control Panel.**

OpenSearch™ is an open-source search and analytics suite used for various use cases related to log and data analysis, real-time monitoring, and application searching. Forked from Elasticsearch and Kibana, OpenSearch™ offers users a highly scalable system with easy access to large amounts of data and integrated metrics.

 

## Requirements

- Access to the [OVHcloud Control Panel](https://us.ovhcloud.com/manager/)
- A [Public Cloud project](https://us.ovhcloud.com/public-cloud/) in your OVHcloud account

 

## Instructions

### Subscribe to the service

From the [OVHcloud Control Panel](https://us.ovhcloud.com/manager/), navigate to the `Data Analysis` section via the `Public Cloud` menu.

Click the `Create a database instance` (`+ Create a service` if your project already contains services) button.

 

#### Step 1: Select your database type

Click on the engine you want to use and then (if available) select the version to install from the drop-down menu.

![manager\_opensearch\_order\_databaseType.png](/images/public-cloud/databases/opensearch-getting-started/43164654486291.png)

 

#### Step 2: Select a service plan

In this step, choose an appropriate service plan. If needed, you will be able to upgrade the plan after creation.

Please see the [Capabilities and limitations](https://support.us.ovhcloud.com/hc/en-us/articles/21913096243219) guide for detailed information on each plan's properties.

![manager\_opensearch\_order\_servicePlan.png](/images/public-cloud/databases/opensearch-getting-started/43164663631891.png)

 

#### Step 3: Select a region

Choose the geographical region of the data center in which your service will be hosted.

![manager\_database\_order\_regions.png](/images/public-cloud/databases/opensearch-getting-started/43164663636499.png)

 

#### Step 4: Select the node type

You can increase the number of nodes and choose the node template in this step. The initial and maximum number of nodes depends on the solution chosen in step 2.

Please visit the [Capabilities and limitations](https://support.us.ovhcloud.com/hc/en-us/articles/21913096243219) guide for detailed information on the hardware resources and other properties of the engine installation.

![manager\_opensearch\_order\_nodeType.png](/images/public-cloud/databases/opensearch-getting-started/43164663638419.png)

 

#### Step 5: Cluster sizing

Here you can add additional storage. Please take note of the pricing information.

![manager\_opensearch\_order\_clusterSizing.png](/images/public-cloud/databases/opensearch-getting-started/43164654494355.png)

 

#### Step 6: Configure your options

Decide whether to attach your instance to a public network or a private network. If you select a **Private network (vRack)**, you will need to select a network and subnetwork to attach.

![manager\_database\_order\_publicNetwork.png](/images/public-cloud/databases/opensearch-getting-started/43164663642771.png)

 

#### Step 7: Review and confirm

The panel on the right side of the screen will display a summary of your order.

In a matter of minutes, your new service will be deployed. Message banners in the OVHcloud Control Panel will inform you when the service is ready to use.

![manager\_opensearch\_order\_summary.png](/images/public-cloud/databases/opensearch-getting-started/43164663644051.png)

 

### Configure the OpenSearch™ service

Once the OpenSearch™ service is up and running, you will have to define at least one user and one authorized IP to fully connect to the service.

 

#### Step 1 (mandatory): Set up a new user

Switch to the `Users` tab. An admin user is pre-configured during the service installation. You can add more users by clicking on the button `+ Add user`.

Enter a username, then click `Create User`.

Once the user is created, the password is generated. Please keep it secure, as it will not be shown again.

![manager\_opensearch\_usersTab.png](/images/public-cloud/databases/opensearch-getting-started/43164663645971.png)

Passwords can be created for the admin user or changed for all afterward by clicking the *more options* `...` button in that user's row of the table.

![manager\_opensearch\_usersTab\_moreOptions.png](/images/public-cloud/databases/opensearch-getting-started/43164663647635.png)

 

#### Step 2 (mandatory): Configure authorized IPs

:::warning

**NOTE:** For security reasons, the default network configuration doesn't allow any incoming connections. You must authorize suitable IP addresses to successfully access your service.

:::

Switch to the `Configuration` tab. At least one IP address must be authorized here before you can connect to your service. It can be your laptop IP, for example.

In the **IPs** panel, enter the information for the IP you wish to authorize and click the *add* `+` button. Click `Save changes` when you are finished.

Clicking on `+ Add an IP address or IP block (CIDR)` opens a new window in which you can add single IP addresses or blocks to allow access to the service.

![manager\_opensearch\_configurationTab.png](/images/public-cloud/databases/opensearch-getting-started/43164654507283.png)

You can edit and remove access to your service via the *delete* `⛔️` button.

If you don't know how to get your IP, please visit a website such as [www.WhatismyIP.com](https://www.whatismyip.com/). Copy the IP address numbers shown on this website and save them for later.

:::tip

**Success!** Your OpenSearch™ service is now fully accessible!

:::

Optionally, you can configure access control lists (ACLs) for granular permissions.

 

#### Optional: Configure ACLs

OpenSearch™ supports index-level access control lists (ACLs) to control permissions. This approach allows you to limit the operations that are available to specific connections and to restrict access to certain data sets, which improves the security of your data.

You first have to enable the ACLs functionality, and then you can add ACLs for all users:

![DBaaS\_opensearch\_gettingstarted12.png](/images/public-cloud/databases/opensearch-getting-started/25067596080147.png)

You can grant the following permissions:

- **Administrator**: full access to APIs and documents
- **Read-only**: allows only searching and retrieving documents
- **Write**: allows updating, adding, and deleting documents
- **Read and write**: full access to documents
- **none**: no access

![DBaaS\_opensearch\_gettingstarted13.png](/images/public-cloud/databases/opensearch-getting-started/25067582247315.png)

:::warning

**NOTE:** Write permission allows the service user to create new indexes that match the pattern, but it does not allow the deletion of those indexes.

:::

Rules are defined separately for each user as **permission/index model** combinations. The **index model**, also called **pattern**, defines the indexes that the permission applies to. Patterns are glob-style, where **\*** matches any number of characters and **?** matches any character.

![DBaaS\_opensearch\_gettingstarted14.png](/images/public-cloud/databases/opensearch-getting-started/25067596105235.png)

When multiple rules match, they are applied in the order listed above. If ACLs are enabled and no rules match, access is denied.

 

##### **Control access to top-level APIs**

OpenSearch™ has several “top-level” API endpoints (*\_mget*, *\_msearch*, and so on), where you have to grant access separately. To do this, use patterns similar to the index patterns, for example:

- Administrator/\_\* would grant unlimited access to all top-level APIs
- Administrator/\_msearch grants unlimited access to the \_msearch API only

 

##### **Access control and OpenSearch™ Dashboards**

Enabling ACLs does not restrict access to OpenSearch™ Dashboards itself, but all requests made by OpenSearch™ Dashboards are checked against the current user’s ACLs.

In practice, for OpenSearch™ Dashboards to work properly, you must grant the user admin-level access to the *\_msearch* interface (permission: Administrator, pattern: *\_msearch*).

 

### Advanced configuration

#### Index patterns

An index contains a schema. Each data is based on a basic unit, which is a JSON document, indexed with a unique ID. Indexing is the method used to store data by the search engine for fast retrieval.

You can learn more by browsing the [OpenSearch™ indexing documentation](https://opensearch.org/docs/latest/opensearch/index-data/).

It's not mandatory, but if needed, you can add new indices.

![manager\_opensearch\_indexPatternsTab.png](/images/public-cloud/databases/opensearch-getting-started/43164654508051.png)

Please enter the name of the pattern that you want to create.

![manager\_opensearch\_indexPatternsTab\_addPattern.png](/images/public-cloud/databases/opensearch-getting-started/43164654509971.png)

 

#### Automatic backups

OVHcloud performs daily backups for your OpenSearch™ service. To find them, switch to the `Backups` tab. Backups will be created automatically, and retention time depends on the service plan. Please read the [Capabilities and limitations](https://support.us.ovhcloud.com/hc/en-us/articles/21913096243219) guide for detailed information on each plan's properties.

You can restore and delete backups via the *more options* `...` button.

![manager\_opensearch\_backupsTab.png](/images/public-cloud/databases/opensearch-getting-started/43164654510995.png)

 

### Connection to the OpenSearch™ Dashboard

:::warning

**NOTE:** Verify that the IP address visible from your browser application is part of the "Authorized IPs" defined for this OpenSearch™ service.

:::

From the *General information* tab in your OpenSearch™ service, you can get the Kibana service URI. Use it to combine with the user login and password in a web browser, and you will go to this page:

![opensearch\_home.png](/images/public-cloud/databases/opensearch-getting-started/43164654515475.png)

 

#### Load sample data

:::warning

**NOTE:** If you have enabled ACLs in a previous step and don't want to disable them, then make sure your user is granted read and write permissions to:

- opensearch\_dashboards\_sample\_data\_\*
- \_bulk
- \_mget

Without these permissions, you cannot add the sample data and view the corresponding dashboard.

:::

As proposed natively in OpenSearch™, we can load sample data to the OpenSearch™ service:

![opensearch\_addData.png](/images/public-cloud/databases/opensearch-getting-started/43164663662355.png)

For the following example, we will use the **Sample web logs**.

Once imported, you can get access to the main OpenSearch™ dashboard created for this sample. Click on the View Data button:

![opensearch\_logs.png](/images/public-cloud/databases/opensearch-getting-started/43164663663251.png)

The dashboard and queries can be edited directly through this web interface.

 

#### Load your data

If you want to load your data, you have multiple ways. Please take a look at the [official OpenSearch™ documentation](https://opensearch.org/docs/latest/clients/agents-and-ingestion-tools/index/).

 

### Query the OpenSearch™ service

#### DB queries with cURL

Once the service is up and configured, you can try to connect from a remote session to it. You need to have at least:

- the user login and password, with accurate ACLs if enabled;
- the URI of the OpenSearch™ service. It can be found in the `Dashboard` tab.

:::warning

**NOTE:** Verify that the IP address of your system running the cURL command is part of the Authorised IPs defined for this OpenSearch™ service.

:::

Once ready, try this in a command line, with your user and password:

```bash
export OPENSEARCH_URI=https://user1:2fakeSVV5wvyPykF@opensearch-682faf00-682faf00.database.cloud.ovh.us:20184
```

Then try to connect:

```bash
curl $OPENSEARCH_URI
```

The response should be similar to:

```json
{
"name" : "opensearch-682faf00-1",
"cluster_name" : "ba1b1006-776a-4686-9c3c-9f65d4d73467",
"cluster_uuid" : "UoyMAkrISd6r1rIVYn4q5g",
"version" : {
"distribution" : "opensearch",
"number" : "1.1.0",
"build_type" : "unknown",
"build_hash" : "unknown",
"build_date" : "2021-11-15T16:09:53.881093Z",
"build_snapshot" : false,
"lucene_version" : "8.9.0",
"minimum_wire_compatibility_version" : "6.8.0",
"minimum_index_compatibility_version" : "6.0.0-beta1"
},
"tagline" : "The OpenSearch Project: https://opensearch.org/"
}
```

Now create an index:

```bash
curl -X PUT $OPENSEARCH_URI/my-index
```

In case of a wrongly defined ACL for the user, the response will be similar to:

```bash
<html><body>
<h1>403 Forbidden</h1>
Request forbidden by administrative rules.<br/>
(reason: PUT /my-index/ not allowed)
</body></html>
```

Try to add a document:

```bash
curl -X PUT $OPENSEARCH_URI/my-index/_doc/1' -H 'Content-Type: application/json' -d '{"Description": "To be or not to be, that is the question."}'
```

Retrieve the data:

```bash
curl -X GET $OPENSEARCH_URI/my-index/_doc/1
```

Delete the data:

```bash
curl -X DELETE $OPENSEARCH_URI/my-index/_doc/1
```

Delete the index:

```bash
curl -X DELETE $OPENSEARCH_URI/my-index/
```

 

#### DB queries with the Dashboard Dev Tool

For direct queries with a web interface to the OpenSearch™ service, you can use the **Dev Tools** available in the menu:

![opensearch\_sideNavigation.png](/images/public-cloud/databases/opensearch-getting-started/43164654520211.png)

The Console is split into two: a left column for the command to execute, and a right column for the results.

Click on the triangle to execute the highlighted request.

![opensearch\_executeRequest1.png](/images/public-cloud/databases/opensearch-getting-started/43164663666707.png)

We can execute the same commands as we did with cURL:

![opensearch\_executeRequest2.png](/images/public-cloud/databases/opensearch-getting-started/43164654527891.png)

 

## Go further

Your service is now configured. To go further, view the [OpenSearch™ documentation](https://opensearch.org/docs/latest) or visit the [GitHub examples repository](https://github.com/ovh/public-cloud-databases-examples/tree/main/databases/opensearch) to find out how to connect to your service with several languages.

For more information and tutorials, please see our other [Managed Databases & Analytics](https://support.us.ovhcloud.com/hc/en-us/sections/20348597107603-Managed-Databases-Analytics) or [Platform as a Service](https://support.us.ovhcloud.com/hc/en-us/categories/1500000438761-Platform-as-a-Service) guides. You can also explore the [guides for other OVHcloud products and services](https://support.us.ovhcloud.com/hc/en-us).
