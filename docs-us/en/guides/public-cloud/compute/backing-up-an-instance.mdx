---
title: "Backing up an instance"
description: "Learn how to create manual and automatic backups of a Public Cloud instance."
zendesk_id: 4460743125395
zendesk_hash: 078d3921fe03ace3
content_hash: 8d3b040c2fe5c391
updated: 2026-01-05
---

import Api from '@components/Api';
import { Tab, Tabs } from '@rspress/core/theme';

# Backing up an instance

**Learn how to create manual and automatic backups of a Public Cloud instance.**

You can create a single backup of an instance or configure a schedule to automate your instance backups. Backups can be used to restore your instance to a previous state or to create a new, identical instance.

 

## Requirements

- A [Public Cloud instance](https://support.us.ovhcloud.com/hc/en-us/articles/360002245164) in your OVHcloud account
- Access to the [OVHcloud Control Panel](https://us.ovhcloud.com/manager/)
- *Optional:* Access to OpenStack CLI (see [this guide](https://support.us.ovhcloud.com/hc/en-us/articles/1500009262762) to get started)

 

## Topics

- [Creating a manual backup of an instance](#creating-a-manual-backup-of-an-instance)

- [Creating an automated backup of an instance](#creating-an-automated-backup-of-an-instance)

- [Managing backups and schedules](#managing-backups-and-schedules)

  - [Manual backups](#manual-backups)
  - [Automatic backups](#automatic-backups)

 

## Instructions

:::info

Two types of backup are available:

- Local: A local backup is stored in the same region as your instance.
- Remote: A remote backup automatically creates a copy of the local backup in a different region of your choice.

Each backup is billed separately — the remote backup will be charged based on the storage pricing of the selected remote region.

**Note:** Local Zones are not eligible for remote backups.

:::

### Creating a manual backup of an instance

<Tabs>

<Tab label="Via the OVHcloud Control Panel">

From the [OVHcloud Control Panel](https://us.ovhcloud.com/manager/), access the `Public Cloud` section and select your Public Cloud project.

Choose the `Instances` section from the left-hand menu.

Click the *more options* `...` button to the right of your instance and choose `Create a backup`.

![PCI\_instances\_moreOptions.png](/images/public-cloud/compute/backing-up-an-instance/46336315068435.png)

<details>
<summary>Local backup</summary>

Enter a **name** for the backup, review the pricing information, and click `Confirm` to finalize.

![PCI\_instances\_createBackup-manual.png](/images/public-cloud/compute/backing-up-an-instance/46336308727827.png)

</details>

<details>
<summary>Remote backup</summary>

Enter a name for the backup, review the pricing information, and click `Add a remote backup`.

Enter a name for the remote backup, select a region, and click `Confirm`.

![PCI\_instances\_createBackup-remote.png](/images/public-cloud/compute/backing-up-an-instance/47311533313171.png)

</details>

It is not possible to track the backup's progress in real time. However, when you click the `Instance Backup` section, you can see the status "Backup in progress" during the process.

Once the backup is complete, the status will change to "Enabled."

![PCI\_instanceBackup\_inProgress.png](/images/public-cloud/compute/backing-up-an-instance/46336315073683.png)

</Tab>

<Tab label="Via the OVHcloud API">

Log in to the [OVHcloud API](https://api.us.ovhcloud.com/).

You can list all available regions using the following API call:

<Api version="v1" section="/cloud" method="GET" route={"/cloud/project/\{ServiceName\}/region"} regions={["us"]} />

Then, use the following API call to create your backup:

<Api version="v1" section="/cloud" method="POST" route={"/cloud/project/\{serviceName\}/instance/\{instanceId\}/snapshot"} regions={["us"]} />

Fill in the variables:

- **instanceId**: The unique instance ID.
- **regionName**: The region name where the source instance is located.
- **serviceName**: The OVHcloud project ID.
- **distantRegionName (optional)**: The  region name where the remote backup will be stored.
- **distantSnapshotName (optional)**: The name of the remote backup to create in the remote region.
- **snapshotName**: The name of the snapshot (local backup) to create.

:::info

Only create a remote backup if the parameters related to the remote region (**distantRegionName** and **distantSnapshotName**) are provided.

:::

You can also confirm that your backup was created successfully:

<Api version="v1" section="/cloud" method="GET" route={"/cloud/project/\{serviceName\}/snapshot"} regions={["us"]} />

</Tab>

<Tab label="Via the OpenStack CLI">

Run the following command to display the list of instances:

```bash
openstack server list

+--------------------------------------+-----------+--------+--------------------------------------------------+--------------+
| ID | Name | Status | Networks | Image Name |
+--------------------------------------+-----------+--------+--------------------------------------------------+--------------+
| aa7115b3-83df-4375-b2ee-19339041dcfa | Server 1 | ACTIVE | Ext-Net=51.xxx.xxx.xxx, 2001:41d0:xxx:xxxx::xxxx | Ubuntu 16.04 |
+--------------------------------------+-----------+--------+--------------------------------------------------+--------------+
```

You can list all available regions using the following command:

```bash
openstack region list
```

<details>
<summary>Local backup</summary>

Then run the following command to create a backup of your instance:

```bash
openstack server image create --name snap_server1 aa7115b3-83df-4375-b2ee-19339041dcfa
```

</details>

<details>
<summary>Remote backup</summary>

Run the following command after following the local backup step:

```bash
openstack workflow execution create ovh. glance.glance_download '{"src_image_id":"<image_id>", "src_region":"<current_region>", "dst_region":"<remote_region>"}'
```

</details>

</Tab>

<Tab label="Via Horizon">

Click on the `Compute` menu on the left, then select `Instances`.\
Then click on the `Create Snapshot` button to the right of the instance line.

![horizon\_instances\_dashboard.png](/images/public-cloud/compute/backing-up-an-instance/46336308734483.png)

Fill in the backup name and press the `Create Snapshot` button.

![horizon\_createSnapshot.png](/images/public-cloud/compute/backing-up-an-instance/46336315080467.png)

</Tab>

</Tabs>

 

### Creating an automated backup of an instance

:::info

If you want to automate this functionality directly via OpenStack, you can create a Mistral workflow associated with a cron trigger.

:::

<Tabs>

<Tab label="Via the OVHcloud Control Panel">

From the [OVHcloud Control Panel](https://us.ovhcloud.com/manager/), access the `Public Cloud` section and select your Public Cloud project.

Choose the `Instances` section from the left-hand menu.

Click the *more options* `...` button to the right of your instance and choose `Create an automatic backup`.

![PCI\_instances\_moreOptions.png](/images/public-cloud/compute/backing-up-an-instance/46336315068435.png)

You can configure the backup settings on the next page.

 

#### The workflow

Currently, only one workflow exists; it will create a backup of the instance and its primary volume.

Select **Automatic instance backup** as your workflow, then click `Next`.

![PCI\_createWorkflow-1.png](/images/public-cloud/compute/backing-up-an-instance/46336308739091.png)

 

#### The resource

Select the targeted instance that you wish to back up, then click `Next`.

![PCI\_createWorkflow-2.png](/images/public-cloud/compute/backing-up-an-instance/46336308740371.png)

 

#### The name

Give the schedule a name, take note of the pricing information, and then click `Create`.

![PCI\_createWorkflow-4.png](/images/public-cloud/compute/backing-up-an-instance/46336315086099.png)

Your backup workflow will be visible in the `Workflow Management` area under the **Compute** heading.

 

#### The schedule

You can set up a custom backup schedule or choose one of the default frequencies:

- Daily backup with retention of the last 7 backups, or
- Daily backup with retention of the last 14 backups.

Click `Next`.

![PCI\_createWorkflow-3\_custom.png](/images/public-cloud/compute/backing-up-an-instance/46336315085203.png)

<details>
<summary>Adding a remote backup</summary>

Click the `Add a remote backup` button, select a location, review the pricing information, and then click the `Create` button.

![PCI\_createWorkflow-4\_remote.PNG](/images/public-cloud/compute/backing-up-an-instance/47933583557139.png)

</details>

</Tab>

<Tab label="Via the OVHcloud API">

From the [OVHcloud API](https://api.us.ovhcloud.com/), you can create an automatic backup workflow using the following API call:

<Api version="v1" section="/cloud/project" method="POST" route={"/cloud/project/\{serviceName\}/region/\{regionName\}/workflow/backup"} regions={["us"]} />

The backups are charged according to the space used, with a granularity of 1GB. Refer to the [Public Cloud pricing page](https://us.ovhcloud.com/public-cloud/prices/) for Instance Backup pricing.

**Parameters:**

|                   |                                                                                                                                                                                    |
| ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Input**         | **Notes**                                                                                                                                                                          |
| serviceName       | API to obtain a list of available services (serviceName):<Api version="v1" section="/cloud/project" method="GET" route={"/cloud/project"} regions={["us"]} />                      |
| regionName        | API to obtain a list of available regions (regionName):<Api version="v1" section="/cloud/project" method="GET" route={"/cloud/project/\{serviceName\}/region"} regions={["us"]} /> |
| instanceId        | API to obtain a list of Instance IDs:<Api version="v1" section="/cloud/project" method="GET" route={"/cloud/project/\{serviceName\}/instance"} regions={["us"]} />                 |
| cron              | Set to Unix cron format (time zone in UTC)                                                                                                                                         |
| maxExecutionCount | Set the number of executions to process before the end of the task. Zero (0) means that the task will never end.                                                                   |
| name              | The name of the automatic backup schedule                                                                                                                                          |
| rotation          | Set the number of backups to retain                                                                                                                                                |

You can verify the creation of your workflow with the following API call:

<Api version="v1" section="/cloud/project" method="GET" route={"/cloud/project/\{serviceName\}/region/\{regionName\}/workflow/backup"} regions={["us"]} />

</Tab>

</Tabs>

 

### Managing backups and schedules

#### Manual backups

:::warning

**NOTE:** Instances and backups are charged separately. Deleting an instance will not stop the charges for instance backups. They must be deleted separately.

:::

:::warning

**NOTE:** You cannot delete an instance backup if an instance that has been spawned from that backup is running at the time of the delete action.

:::

From the [OVHcloud Control Panel](https://us.ovhcloud.com/manager/), access the `Public Cloud` section and select your Public Cloud project.

Choose the `Instance Backup` section from the left-hand menu.

Click the *more options* `...` button to the right of your backup.

From here, you can `Create an instance` ([full instructions](https://support.us.ovhcloud.com/hc/en-us/articles/6755292759571)) or `Delete` the backup.

![PCI\_instanceBackup\_moreOptions.png](/images/public-cloud/compute/backing-up-an-instance/46336315087507.png)

**Via OVHcloud API**

<Api version="v1" section="/cloud" method="DELETE" route={"/cloud/project/\{serviceName\}/snapshot/\{snapshotId\}"} regions={["us"]} />

 

#### Automatic backups

:::warning

**NOTE:** Instances and workflows are charged separately. However, if you delete an instance, all workflows attached to it will be deleted automatically (after approx. 30 minutes).

:::

From the [OVHcloud Control Panel](https://us.ovhcloud.com/manager/), access the `Public Cloud` section and select your Public Cloud project.

Choose the `Workflow Management` section from the left-hand menu.

Click the *more options* `...` button to the right of your backup schedule.

From here, you can `Delete` the backup.

![PCI\_deleteWorkflow.png](/images/public-cloud/compute/backing-up-an-instance/46336315088531.png)

**Via OVHcloud API**

<Api version="v1" section="/cloud/project" method="DELETE" route={"/cloud/project/\{serviceName\}/region/\{regionName\}/workflow/backup/\{backupWorkflowId\}"} regions={["us"]} />

 

## Go further

For more information and tutorials, please see our other [Public Cloud support guides](https://support.us.ovhcloud.com/hc/en-us/categories/115000515130-Public-Cloud-Services) or explore the [guides for other OVHcloud products and services](https://support.us.ovhcloud.com/hc/en-us).
