---
title: "How to configure reverse DNS for a Public Cloud instance"
description: "Reverse DNS (rDNS) is the complement to \"forward\" DNS resolution, which resolves domain names into IP addresses. With reverse DNS resolution, an IP address can"
zendesk_id: 23768300812435
zendesk_hash: 55cc60d90705f08d
content_hash: 92e9988055ce6655
updated: 2026-07-23
---

# How to configure reverse DNS for a Public Cloud instance

Reverse DNS (*rDNS*) is the complement to "forward" DNS resolution, which resolves domain names into IP addresses. With reverse DNS resolution, an IP address can resolve into the domain name (or host name) it is mapped to. This means that DNS queries of the associated IP address will return this domain name.

Configuring the reverse DNS resolution for an instance is especially useful when sending emails. A mail server's validation by spam protection systems will improve if a DNS lookup of the IP address resolves properly.

 

## Requirements

- A [Public Cloud instance](https://us.ovhcloud.com/public-cloud/) in your OVHcloud account
- A domain name with its `A` record pointing to the instance

#### OVHcloud Control Panel Access

- **Direct link:** [Network IPs](https://manager.us.ovhcloud.com/#/network/ip)
- **Navigation path:** `Bare Metal Cloud` > `Network` > `IP`

 

## Instructions

Once you see your list of IPs, you can filter for your Public Cloud project ID using the drop-down menus above the table.

In the table, select the *more options* `...` button in the row of an eligible service and select `Configure the reverse DNS`.

![IP\_serviceList.png](/images/public-cloud/compute/how-to-configure-reverse-dns-for-a-public-cloud-instance/53734135407379.png)

In the new window, enter your reverse path and click on `Confirm`.

![IP\_configureReverseDNS.png](/images/public-cloud/compute/how-to-configure-reverse-dns-for-a-public-cloud-instance/53734792625171.png)

You can also edit the reverse path directly via the icon in the **Reverse DNS** column of the table.

:::info

If the modification does not work as expected, verify that the `A` record is correctly configured in the DNS zone of your domain name. Bear in mind that it might take up to 24 hours for DNS zone changes to be effective, in case you have only recently edited the `A` record.

:::

 

## Go further

For more information and tutorials, please see our other [Public Cloud support guides](https://support.us.ovhcloud.com/hc/en-us/categories/115000515130-Public-Cloud-Services) or explore the [guides for other OVHcloud products and services](https://support.us.ovhcloud.com/hc/en-us).
