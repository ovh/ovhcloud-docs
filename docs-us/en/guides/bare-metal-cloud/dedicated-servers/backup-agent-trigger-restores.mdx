---
title: "Backup Agent - Trigger restores"
description: "We recommend using the File-level restore (FLR) wizard. This is the primary and most common method. It lets you restore one or more files or folders from a back"
zendesk_id: 52099397586963
zendesk_hash: bbace080f2692bdd
content_hash: 992365e3a691d3b7
updated: 2026-08-17
---

import { Tab, Tabs } from '@rspress/core/theme';

# Backup Agent - Trigger restores

We recommend using the File-level restore (FLR) wizard. This is the primary and most common method. It lets you restore one or more files or folders from a backup, without restarting the server.

**WARNING:** Full server restore from a recovery ISO (Bare Metal Recovery) is not supported and, therefore, is outside the scope of this guide; our customer support teams cannot assist with it.

:::info

For more information about the Backup Agent product, see our [Backup Agent - Product Overview](https://support.us.ovhcloud.com/hc/en-us/articles/51706411167635) guide.

:::

 

## Requirements

- A Bare Metal server with the Backup Agent installed and configured on it (see [this guide](https://support.us.ovhcloud.com/hc/en-us/articles/52033454950291) for more information)
- At least one backup has been performed (see [this guide](https://support.us.ovhcloud.com/hc/en-us/articles/52047862812819) for more information)

 

## Instructions

### File-level restore (FLR wizard)

This is the primary method to recover one or more files or folders from a backup.

To restore files and folders, click on the tab corresponding to your Operating System:

<Tabs>

<Tab label="Windows">

Open the "Veeam Agent" application on your Bare Metal server:

![Backup Agent BKP Agent Search](/images/storage-and-backup/backup-agent/backup-agent-backup-restore/01-backup-agent-bkpagent-search.png)

Go to the menu and select `Restore File`:

![Backup Agent Restore Menu](/images/storage-and-backup/backup-agent/backup-agent-backup-restore/01-backup-agent-restore-menu.png)

Select the desired restore point in the wizard:

![Backup Agent Restore Points](/images/storage-and-backup/backup-agent/backup-agent-backup-restore/01-backup-agent-restore-restore-points.png)

Then confirm:

![Backup Agent Restore Point Summary](/images/storage-and-backup/backup-agent/backup-agent-backup-restore/01-backup-agent-restore-restore-point-summary.png)

Finally, search for your file and select an option:

![Backup Agent Restore Wizard](/images/storage-and-backup/backup-agent/backup-agent-backup-restore/01-backup-agent-restore-wizard.png)

- **Restore > Overwrite:** restores the file while overwriting the one currently on the server.
- **Restore > Keep:** restores the file while keeping the one currently on the server.
- **Copy To:** copies the file to a location on your server.
- **Explore:** explores the backup.
- **Properties:** displays the file properties.

Launching a restore will give you a final window that will show the transfer:

![Backup Agent Restore Transfer](/images/storage-and-backup/backup-agent/backup-agent-backup-restore/01-backup-agent-restore-transfer.png)

</Tab>

<Tab label="Linux">

To restore files and folders on Linux, you have two options: via the graphical interface or via the command line.

 

#### Via the graphical interface

1. Connect to your Bare Metal server via SSH.

2. Launch the Veeam interface by typing the following command:

   ```
   sudo veeam
   ```

3. In the Veeam Agent control panel, press the `R` key to access the File Level Restore wizard.

4. In the Imported Backups pane, use the `Up` and `Down` keys to select the backup whose data you wish to restore.

5. In the Restore Points pane, use the `Up` and `Down` keys to select the restore point and press `Enter`.

6. The Veeam Agent will mount the content of the backup file to the `/mnt/backup` directory in the computer's file system. Press `Enter` to return to the Veeam Agent control panel.

 

#### Via the command line

To restore files via the command line, you must first mount the backup:

1. List your available backups (use `--all` to show all imported backups:

   ```
   sudo veeamconfig backup list --all
   ```

2. List the restore points of a backup:

   ```
   sudo veeamconfig backup info --id <backup_id>
   ```

3. Mount a restore point:

   :::info

   The `point_id` is the `OIB ID` from the previous command's output.

   :::

   ```
   sudo veeamconfig point mount --id <point_id>
   ```

   This will output confirmation of the mount and state the session id.

   ```
   Restore point is mounted.
   Session ID: [{<session_id>}].
   Logs stored in: [/var/log/veeam/Mount/Session_########_######_{<session_id>}].
   ```

4. Once mounted, you can access the files via the mount point (usually in `/mnt/backup/`).

5. Copy the desired files from the mount point to their destination.

6. Once the restore is complete, use the session ID to unmount the backup:

   ```
   sudo veeamconfig session stop --id <session_id>
   ```

For more information, see the [Veeam documentation](https://helpcenter.veeam.com/docs/agentforlinux/userguide/files_restore_gui.html?ver=13) and the [command line restore documentation](https://helpcenter.veeam.com/docs/agentforlinux/userguide/files_restore_cmd.html?ver=13).

</Tab>

</Tabs>

 

## Go further

For more information and tutorials, please see our other [Dedicated Servers support guides](https://support.us.ovhcloud.com/hc/en-us/categories/115000423390-Dedicated-Servers) or explore the [guides for other OVHcloud products and services](https://support.us.ovhcloud.com/hc/en-us).
