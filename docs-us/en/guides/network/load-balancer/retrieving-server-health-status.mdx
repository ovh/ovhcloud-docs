---
title: "Retrieving server health status"
description: "The OVHcloud Load Balancer service acts by default as a proxy. That is to say, it distributes the load (requests) it receives between all the servers of the des"
zendesk_id: 29025970501779
zendesk_hash: 4ee35235934045f4
content_hash: 0f64bec691faee02
updated: 2025-12-17
---

import Api from '@components/Api';

# Retrieving server health status

The OVHcloud Load Balancer service acts by default as a proxy. That is to say, it distributes the load (requests) it receives between all the servers of the desired farm.

Each server can be configured for the load balancer to check its status frequently.

Once a server is detected as "down," the load balancer no longer sends any data to it and distributes the load among the remaining servers.

This can be useful in the case of server maintenance: you can take the server out of the farm, perform maintenance, and then reintegrate it into the farm.

However, when a server is removed from the farm by the load balancer beyond our control, it can be interesting to know about it and why.

 

## Requirements

- An [OVHcloud Load Balancer](https://us.ovhcloud.com/network/load-balancer/) offer in your OVHcloud account
- Access to the [OVHcloud API](https://api.us.ovhcloud.com/)
- A configured farm
- A configured front-end

 

## Instructions

To have valid server status checks, you need to have a probe configured on your farm and servers that allow access to that probe.

If you need to configure probes on your Load Balancer, please refer to [this guide](https://support.us.ovhcloud.com/hc/en-us/articles/29115526049811).

### Via the OVHcloud API

In the API, server health status is available in the serverState table:

<Api version="v1" section="/ipLoadbalancing" method="GET" route={"/ipLoadbalancing/\{serviceName\}/http/farm/\{farmId\}/server/\{serverId\}"} regions={["us"]} />

<Api version="v1" section="/ipLoadbalancing" method="GET" route={"/ipLoadbalancing/\{serviceName\}/tcp/farm/\{farmId\}/server/\{serverId\}"} regions={["us"]} />

**Example result:**

![IPLB\_serverHealthStatus\_API.png](/images/network/load-balancer/retrieving-server-health-status/47487547997075.png)

 

#### Via the OVHcloud Control Panel

:::warning

**NOTE:** Although there is a **Status** column for your servers in the OVHcloud Control Panel, there is a known bug preventing the retrieval of the health status. Please rely on the OVHcloud API for accurate server statuses.

:::

 

## Go further

For more information and tutorials, please see our other [OVHcloud Load Balancer guides](https://support.us.ovhcloud.com/hc/en-us/sections/20357562381843-Load-Balancer) or explore the [guides for other OVHcloud products and services](https://support.us.ovhcloud.com/hc/en-us).
