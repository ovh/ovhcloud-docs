---
title: "OVHcloud Connect - Configuration using the OVHcloud APIv6"
description: "NOTE: To ensure the correct operation of this service, you must be aware of the technical capabilities and limitations of the OVHcloud Connect solution and conf"
zendesk_id: 7007737758867
zendesk_hash: 6809c3deba946d37
content_hash: 30e6211a091d9932
updated: 2025-06-11
---

import Api from '@components/Api';
import { Tab, Tabs } from '@rspress/core/theme';

# OVHcloud Connect - Configuration using the OVHcloud APIv6

## Requirements

:::warning

**NOTE:** To ensure the correct operation of this service, you must be aware of the [technical capabilities and limitations of the OVHcloud Connect solution](https://support.us.ovhcloud.com/hc/en-us/articles/7048308555667) and configure your network devices accordingly.

:::

- An [OVHcloud Connect service](https://us.ovhcloud.com/network/ovhcloud-connect/)
- Access to the [OVHcloud APIv6](https://api.us.ovhcloud.com/console) (create your credentials by consulting [this guide](https://support.us.ovhcloud.com/hc/en-us/articles/360018130839))

 

## Instructions

### Step 1: Configuring vRack

As a mandatory first step, the service must be interconnected with a vRack to enable the configuration.

To get the UUIDs of eligible services, use the following call:

<Api version="v1" section="/vrack" method="GET" route={"/vrack/\{serviceName\}/ovhCloudConnect"} regions={["us"]} />

To link your OVHcloud Connect service with the vRack:

<Api version="v1" section="/vrack" method="POST" route={"/vrack/\{serviceName\}/ovhCloudConnect"} regions={["us"]} />

<details>
<summary>Information required:</summary>

- The **serviceName** of your vRack
- The **ovhCloudConnect** UUID

</details>

 

### Step 2: Configuring the PoP

This step is important because you have to choose between L2 and L3.

:::warning

**NOTE:&#x20;**&#x50;lease be aware of the ramifications of this decision. To switch between L2 and L3 later, you will have to delete the whole configuration.

:::

#### Obtaining the interface ID

To obtain the ID of the interface dedicated to your service, use the following call:

<Api version="v1" section="/ovhCloudConnect" method="GET" route={"/ovhCloudConnect/\{serviceName\}/interface"} regions={["us"]} />

The following call provides more service details:

<Api version="v1" section="/ovhCloudConnect" method="GET" route={"/ovhCloudConnect/\{serviceName\}/interface/\{id\}"} regions={["us"]} />

:::info

The LightStatus parameter is refreshed every 5 minutes for monitoring purposes.

:::

 

#### Configuration

<Tabs>

<Tab label="Layer 2">

To create a Layer 2 configuration, use the following call:

<Api version="v1" section="/ovhCloudConnect" method="POST" route={"/ovhCloudConnect/\{serviceName\}/config/pop"} regions={["us"]} />

<details>
<summary>Information required:</summary>

- Your OVHcloud Connec&#x74;**&#x20;serviceName**
- The **interfaceID** found in [the previous step](#obtaining-the-interface-id)
- **type:** "l2"

</details>

</Tab>

<Tab label="Layer 3">

The Layer 3 configuration is more complex because of the required BGP settings:

<Api version="v1" section="/ovhCloudConnect" method="POST" route={"/ovhCloudConnect/\{serviceName\}/config/pop"} regions={["us"]} />

<details>
<summary>Information required:</summary>

- Your OVHcloud Connec&#x74;**&#x20;serviceName**
- The **customerBgpArea** (your BGP ASN, configured on your device, which will be used for peering)
- The **interfaceID** found in [the previous step](#obtaining-the-interface-id)
- The **ovhBgpArea** (your BGP ASN, configured on the OVHcloud routing instance, pertaining to BGP session and AS path)
- **subnet** (a /30 IPv4 block)
- **type:** "l3"

</details>

</Tab>

</Tabs>

 

### Step 3: Configuring the data center (DC)

:::info

If an OVHcloud Connect service is already configured in the vRack, the second service will inherit the data center configuration.

:::

#### Obtaining available data centers

You can list available data centers for configuration using the following calls:

<Api version="v1" section="/ovhCloudConnect" method="GET" route={"/ovhCloudConnect/\{serviceName\}/datacenter"} regions={["us"]} />

<details>
<summary>Information required:</summary>

- Your OVHcloud Connec&#x74;**&#x20;serviceName**

</details>

<Api version="v1" section="/ovhCloudConnect" method="GET" route={"/ovhCloudConnect/\{serviceName\}/datacenter/\{id\}"} regions={["us"]} />

<details>
<summary>Information required:</summary>

- The **id** of your datacenter
- Your OVHcloud Connec&#x74;**&#x20;serviceName**

</details>

 

#### Configuration

<Tabs>

<Tab label="Layer 2">

Only the ID of the data center is needed for the L2 configuration:

<Api version="v1" section="/ovhCloudConnect" method="POST" route={"/ovhCloudConnect/\{serviceName\}/config/pop/\{popId\}/datacenter"} regions={["us"]} />

<details>
<summary>Information required:</summary>

- The **popId** created in [Step 2](#step-2-configuring-the-pop)
- Your OVHcloud Connec&#x74;**&#x20;serviceName**
- The **datacenterId** [previously obtained](#obtaining-available-data-centers)

</details>

</Tab>

<Tab label="Layer 3">

More parameters have to be provided for the L3 configuration:

<Api version="v1" section="/ovhCloudConnect" method="POST" route={"/ovhCloudConnect/\{serviceName\}/config/pop/\{popId\}/datacenter"} regions={["us"]} />

<details>
<summary>Information required:</summary>

- The **popId** created in [Step 2](#step-2-configuring-the-pop).
- Your OVHcloud Connec&#x74;**&#x20;serviceName**
- The **datacenterId** [previously obtained](#obtaining-available-data-centers)
- The **ovhBgpArea** (you need to assign an ASN for the OVHcloud routing instance; it will appear in AS path and can be different from the PoP ASN)
- The **subnet** (an IPv4 block; sizes starting at /28 are accepted)

</details>

By default, the data centre will be configured with a VRRP instance. You have to proceed with the next steps for static routing or dynamic routing using BGP.

 

**Layer 3 option: Static route**

A static route is needed when you have one or more subnets behind a gateway. This may be a Linux gateway (with IP forwarding enabled), an NSX edge, or any instance capable of routing.

<Api version="v1" section="/ovhCloudConnect" method="POST /OVHCLOUDCONNECT/{SERVICENAME}/CONFIG/POP/{POPID}/DATACENTER/{DATACENTERID}/EXTRA" route={"/ovhCloudConnect/\{serviceName\}/config/pop/\{popId\}/datacenter/\{datacenterId\}/extra"} regions={["us"]} />

<details>
<summary>Information required:</summary>

- The **datacenterId** [previously obtained](#obtaining-available-data-centers)
- The **popId** created in [Step 2](#step-2-configuring-the-pop).
- Your OVHcloud Connec&#x74;**&#x20;serviceName**
- **nextHop** (the IP address in the subnet range acting as the gateway)
- **subnet** (a prefix using the CIDR notation)
- **type:** "network"

</details>

 

**Layer 3 option: BGP session**

A BGP session enables dynamic routing from your routing instance with OVHcloud Connect. Announcements are dynamically managed using the BGP protocol. Enabling a BGP session disables the VRRP configuration. You cannot have a BGP session and a static route in the same DC configuration.

<Api version="v1" section="/ovhCloudConnect" method="POST" route={"/ovhCloudConnect/\{serviceName\}/config/pop/\{popId\}/datacenter/\{datacenterId\}/extra"} regions={["us"]} />

<details>
<summary>Information required:</summary>

- The **datacenterId** [previously obtained](#obtaining-available-data-centers)
- The **popId** created in [Step 2](#step-2-configuring-the-pop).
- Your OVHcloud Connec&#x74;**&#x20;serviceName**
- **bgpNeighborArea** (your BGP ASN)
- **bgpNeighborIp** (your IP address in the subnet range)
- **type:** "bgp"

</details>

</Tab>

</Tabs>

 

### Removing resources

Each resource can be deleted individually, but deleting a parent resource like a DC or PoP will automatically delete all sub-resources. However, recursive deletion is slower than a sequential deletion of each resource.

 

#### Global deletion

The following call recursively deletes the entire configuration of an OVHcloud Connect service:

<Api version="v1" section="/ovhCloudConnect" method="DELETE" route={"/ovhCloudConnect/\{serviceName\}/config/pop/\{popId\}"} regions={["us"]} />

Each sub-resource’s status will change from `active` to `toDelete`, but it takes some time to see the status change.

Only one task ID is created.

 

#### Deleting by resource

Each resource can be deleted individually using the following call that will delete the smallest resource (extra):

<Api version="v1" section="/ovhCloudConnect" method="DELETE" route={"/ovhCloudConnect/\{serviceName\}/config/pop/\{popId\}/datacenter/\{datacenterId\}/extra/\{extraId\}"} regions={["us"]} />

The following call removes the DC configuration and recursively deletes any additional sub-resources:

<Api version="v1" section="/ovhCloudConnect" method="DELETE" route={"/ovhCloudConnect/\{serviceName\}/config/pop/\{popId\}/datacenter/\{datacenterId\}"} regions={["us"]} />

When all sub-resources have been deleted, the PoP configuration can be safely removed:

<Api version="v1" section="/ovhCloudConnect" method="DELETE" route={"/ovhCloudConnect/\{serviceName\}/config/pop/\{popId\}"} regions={["us"]} />

:::info

If a DC configuration is shared between two or more OVHcloud Connect services, deleting the PoP configuration of only one will not affect the DC resource.

:::

 

## Go further

For more information and tutorials, please see our other [Network support guides](https://support.us.ovhcloud.com/hc/en-us/categories/115000516864-Network) or explore the [guides for other OVHcloud products and services](https://support.us.ovhcloud.com/hc/en-us).

If you need training or technical assistance to implement our solutions, contact your sales representative or click on [this link](https://us.ovhcloud.com/support/professional-services/) to get a quote and ask our Professional Services experts for a custom analysis of your project.
