---
title: "How to Configure Reverse DNS in the OVHcloud API"
description: "Reverse DNS allows you to map your domain name directly to your OVHcloud service. In this article, we will show you how to configure reverse DNS from the OVHclo"
zendesk_id: 4416448996115
zendesk_hash: 8f049c46007a0dc8
content_hash: 8a742f5f6148d911
updated: 2023-09-05
---

import Api from '@components/Api';

# How to Configure Reverse DNS in the OVHcloud API

Reverse DNS allows you to map your domain name directly to your OVHcloud service. In this article, we will show you how to configure reverse DNS from the OVHcloud API.

### **Prerequisites**

- A domain name with an A record pointing to an OVHcloud IP address

### **Topics**

- Configuring Reverse DNS in the OVHcloud API

### **Configuring Reverse DNS in the OVHcloud API**

**Note:** In this article, we use an example domain name and IP address. Change your inputs accordingly.

To begin configuring reverse DNS, log into the [OVHcloud API](https://api.us.ovhcloud.com/) using the same credentials you use for the OVHcloud Control Panel. Use the following API command and fill in the relevant fields:

<Api version="v1" section="/ip" method="POST" route={"/ip/\{ip\}/reverse"} regions={["us"]} />

![reversednsapi1.png](/images/network/network-tools/how-to-configure-reverse-dns-in-the-ovhcloud-api/reversednsapi1-png.png)

Press **Execute** when you have confirmed that the information is correct. The output should look like this:

```
{
   ipReverse: "203.0.113.1"
   reverse: "ovhcloudtest.com."
}
```

To confirm that the reverse DNS has been configured, use the following command in the OVHcloud API:

<Api version="v1" section="/ip" method="GET" route={"/ip/\{ip\}/reverse/ipReverse"} regions={["us"]} />

![reversednsapi2.png](/images/network/network-tools/how-to-configure-reverse-dns-in-the-ovhcloud-api/reversednsapi2-png.png)

The output should look like this:

```
{
   reverse: "ovhcloudtest.com.",
   ipReverse: "203.0.113.1"
}
```

### **Conclusion**

Having read this article, you should be able to configure reverse DNS on any valid OVHcloud IP address from the OVHcloud API.
