---
title: Understanding link aggregation on dedicated servers
description: Find out how link aggregation works on OVHcloud dedicated servers, what OVHcloud Link Aggregation (OLA) changes, and which guide to follow for your operating system
---

## Objective

Link aggregation bonds several of your server's network interfaces together, using the IEEE 802.3ad standard, also known as Link Aggregation Control Protocol (LACP). Your network links become redundant: if one link goes down, traffic is automatically redirected to another available link. The available bandwidth is increased as well.

Several guides cover link aggregation, depending on your server's range, its operating system, and whether it keeps public connectivity.

**This guide explains how the default aggregation and OVHcloud Link Aggregation (OLA) differ, and points you to the guide matching your setup.**

## Requirements

- A [dedicated server](/links/bare-metal/bare-metal) that supports link aggregation, for example from the **Scale** or **High Grade** ranges, or a third-generation or newer **Advance** server
- An operating system or hypervisor that supports the 802.3ad aggregation protocol (LACP)

## Instructions

### Which interfaces can be aggregated

Servers from the **Scale** and **High Grade** ranges have four network interfaces. In their default configuration, these are two public and two private (vRack) ones. Both aggregations are already enabled on the OVHcloud network, so the operating system only has to create the matching bonds.

Other servers that support link aggregation have a single public and a single private interface.

### Choosing an aggregation mode

[OVHcloud Link Aggregation (OLA)](/guides/bare-metal-cloud/dedicated-servers/ola-enable-manager) is an option that you enable in the OVHcloud Control Panel: it groups all of the server's interfaces into a single private aggregation, instead of keeping the public and the private ones separate.

|Mode|Public connectivity|
|---|---|
|Default|The public bond, or the lone public interface, carries the public IP address|
|OLA|None: the public IP address is no longer accessible|

The bonds that each mode creates depend on how many interfaces the server has. On a server with a single interface of each kind, only OLA creates a bond, over those two interfaces.

**Four interfaces, on the Scale and High Grade ranges:**

<img className="thumbnail" alt="On a four-interface server, the default configuration bonds the two public interfaces together to carry the public IP address, and the two private interfaces together for vRack use. With OLA, all four interfaces form a single bond for vRack use." src="/images/bare-metal-cloud/dedicated-servers/link-aggregation-overview/four-interfaces.png" loading="lazy" />

**Two interfaces:**

<img className="thumbnail" alt="On a two-interface server, the default configuration creates no bond: the public interface carries the public IP address and the private one is used for the vRack. With OLA, both interfaces form a single bond for vRack use." src="/images/bare-metal-cloud/dedicated-servers/link-aggregation-overview/two-interfaces.png" loading="lazy" />

:::warning
A server whose interfaces are fully aggregated for vRack use has no public network interface left, so it can no longer reach the OVHcloud netboot service. See [Manage your server reboot with the OVHcloud Link Aggregation feature](/guides/bare-metal-cloud/dedicated-servers/pxe-with-full-private-dedicated) before enabling OLA.

:::


### Configuring the bonds during the installation

On **Scale** and **High Grade** servers, the installation can configure the bonds for you, so that the server comes up already aggregated.

The bonds are **always** configured, and the option is **not offered**, on:

- **Ubuntu Server** 26.04

The option is offered, and **disabled by default**, on:

- **AlmaLinux**
- **Debian**, except Debian 11
- **Fedora**
- **Proxmox Virtual Environment** and **Proxmox Backup Server**
- **Rocky Linux**, except Rocky Linux 8
- **Ubuntu Server** 22.04 and 24.04
- **Windows Server**, except the Hyper-V images
- [Bring Your Own Image](/guides/bare-metal-cloud/dedicated-servers/bring-your-own-image) and [Bring Your Own Linux](/guides/bare-metal-cloud/dedicated-servers/bring-your-own-linux)

To enable it:

- in the OVHcloud Control Panel, check the corresponding box in the installation wizard
- through the API, set the `enableLacpBonding` customization, as described in [Install an OS via the OVHcloud API](/guides/bare-metal-cloud/dedicated-servers/api-os-installation)

Any other operating system does not configure the bonds during the installation, so you have to configure them yourself afterwards.

:::info
The option can be set on any server, but it only has an effect if the server actually has interfaces to bond.

:::


### Configuring the bonds yourself

If your operating system does not offer the option, or if you would rather configure the bonds after the installation, follow the guide matching your system:

|Operating system|Guide|
|---|---|
|Debian 12 or newer, Ubuntu 24.04 or newer|[Netplan configuration](/guides/bare-metal-cloud/dedicated-servers/lacp-enable-netplan)|
|Debian 9 to 11, rescue mode, and custom images using `ifupdown`|[ifupdown configuration](/guides/bare-metal-cloud/dedicated-servers/lacp-enable-ifupdown)|
|SLES 15|[SLES 15 configuration](/guides/bare-metal-cloud/dedicated-servers/lacp-enable-sles15)|
|Windows Server 2019|[NIC teaming configuration](/guides/bare-metal-cloud/dedicated-servers/ola-enable-w2k19)|
|Proxmox VE|[Proxmox VE networking](/guides/bare-metal-cloud/dedicated-servers/proxmox-network-hg-scale)|
|Windows Server with Hyper-V|[Hyper-V networking](/guides/bare-metal-cloud/dedicated-servers/hyperv-network-hg-scale)|

### How an aggregation is spread across network cards

On **Scale** and **High Grade** servers, an aggregation is spread across two distinct network cards, so that it survives the failure of a whole card and not only that of a switch. For more information, see [Improve Network Resilience on Bare Metal servers](/guides/bare-metal-cloud/dedicated-servers/lacp-resilience-scale-hg).

## Go further

Join our [community of users](/links/community).
