---
title: Signing and encrypting Exchange emails with S/MIME
description: Publish your S/MIME certificate from Outlook, then sign and encrypt your Exchange emails in Outlook, the OWA webmail, iOS and Android
lastUpdated: 2026-09-09
availableIn: [eu, ca]
---

import { Tab, Tabs } from '@rspress/core/theme';

## Objective

S/MIME adds two guarantees to your Exchange emails, using a personal certificate:

- **Encryption** ensures that only the intended recipient can read the message. If the email is intercepted in transit, or if an unauthorised person gains access to it, its content stays unreadable.
- **Digital signature** ensures the identity of the sender and the integrity of the message. It proves that the email really was sent by the expected sender, and that its content has not been altered between sending and reception.

The two are independent: an email can be signed, encrypted, or both.

S/MIME relies on a **certificate specific to each user**, containing a public key and a private key. OVHcloud does not issue S/MIME certificates: you order yours from a certification authority (Sectigo, for example) for the primary email address of your Exchange account.

**This guide explains how to publish your S/MIME certificate and how to sign and encrypt your emails in Outlook, in the OWA webmail, and on mobile devices.**

## Requirements

- A [Hosted Exchange](/links/web/emails-hosted-exchange), [Private Exchange](/links/web/emails-private-exchange) or [Trusted Exchange](/links/web/emails-trusted-exchange) service
- An S/MIME certificate issued for the **primary email address** of your Exchange account
- Outlook for Windows installed and [configured with your Exchange account](/guides/web-cloud/email-and-collaborative-solutions/microsoft-exchange/how-to-configure-outlook-2016)
- To encrypt an email: the certificate of each recipient (see [How encryption works](#how-encryption-works))

:::warning
**Start with Outlook, whichever client you plan to use.** Publishing your certificate to the Exchange organisation can only be done from Outlook for Windows. Until that is done, the other users of your organisation cannot encrypt emails addressed to you, nor verify your signatures — and the other clients (OWA, iOS, Android) cannot be used for S/MIME.

:::

## Instructions

### Step 1: publishing your certificate from Outlook

Publishing your certificate makes it accessible within your Exchange organisation. The other users then retrieve it automatically to encrypt the emails they send you, and to verify the digital signature of the messages you send them. Once this is done, you can use the other email clients.

{/* DRAFT: To verify with the product team — the Trust Center section name ("Email Security") and the group heading above the Settings... button ("Encrypted email") are not shown in any source screenshot; they were supplied from the standard Outlook layout. Confirm the wording for the Outlook versions we support. */}

**Follow the four steps below in order**, in the tabs:

<Tabs>
  <Tab label="1. Trust Center">
    In Outlook, click <code className="action">File</code> > <code className="action">Options</code> > <code className="action">Trust Center</code> > <code className="action">Trust Center Settings...</code>, then open the <code className="action">Email Security</code> section.

    <img className="thumbnail" alt="Outlook Options window with Trust Center selected and the Trust Center Settings button" src="/images/web-cloud/email-and-collaborative-solutions/microsoft-exchange/feature-smime/smime-outlook-trust-center.png" loading="lazy" />

    {/* DRAFT: Reconstructed screenshot (HTML mock), not a capture of a live Outlook: replace with a real screenshot from the product team. */}
  </Tab>
  <Tab label="2. Import the certificate">
    In the <code className="action">Digital IDs (Certificates)</code> area, click <code className="action">Import/Export...</code>. Select <code className="action">Import existing Digital ID from a file</code>, browse to your certificate file, enter the password protecting it, then confirm with <code className="action">OK</code>.

    <img className="thumbnail" alt="Import/Export Digital ID window with the option to import an existing Digital ID from a file selected" src="/images/web-cloud/email-and-collaborative-solutions/microsoft-exchange/feature-smime/smime-outlook-import-certificate.png" loading="lazy" />

    {/* DRAFT: Reconstructed screenshot (HTML mock), not a capture of a live Outlook: replace with a real screenshot from the product team. */}
  </Tab>
  <Tab label="3. Security settings">
    Back in the <code className="action">Email Security</code> section, click <code className="action">Settings...</code> under <code className="action">Encrypted email</code>. Check that the imported certificate is selected as the <code className="action">Signing Certificate</code> and as the <code className="action">Encryption Certificate</code>. Set <code className="action">Hash Algorithm</code> to <code className="action">SHA256</code>, then confirm with <code className="action">OK</code>.

    <img className="thumbnail" alt="Change Security Settings window with the signing and encryption certificates and SHA256 as hash algorithm" src="/images/web-cloud/email-and-collaborative-solutions/microsoft-exchange/feature-smime/smime-outlook-security-settings.png" loading="lazy" />

    {/* DRAFT: Reconstructed screenshot (HTML mock), not a capture of a live Outlook: replace with a real screenshot from the product team. */}

    :::warning
    **Do not keep SHA1 as the hash algorithm.** SHA1 is deprecated for digital signatures: collisions have been demonstrated against it, so a signature computed with SHA1 no longer guarantees the integrity of the message. Choose SHA256 or a stronger algorithm.

    :::
  </Tab>
  <Tab label="4. Publish to the GAL">
    In the <code className="action">Digital IDs (Certificates)</code> area, click <code className="action">Publish to GAL...</code> to publish your certificate to the Global Address List of your organisation, then confirm.

    <img className="thumbnail" alt="Digital IDs area with the Publish to GAL and Import/Export buttons" src="/images/web-cloud/email-and-collaborative-solutions/microsoft-exchange/feature-smime/smime-outlook-publish-gal.png" loading="lazy" />
  </Tab>
</Tabs>

Your certificate is now published. In Outlook, you can sign and encrypt a message from the <code className="action">Options</code> tab of the message editing window, using <code className="action">Sign</code> and <code className="action">Encrypt</code>.

{/* DRAFT: To verify with the product team — exact wording and location of the sign/encrypt controls in the message window for the Outlook versions we support. The source draft does not cover this step. */}

When an email is signed and/or encrypted, the message header indicates it: a padlock icon for encryption and a rosette icon for the signature appear on the right, and a <code className="action">Signed By</code> line shows the address the signature belongs to.

{/* DRAFT: Screenshot to produce — a message header showing the encryption and signature icons. The source screenshot was removed: it showed internal test-environment data and hand-drawn annotations. */}

### Step 2: configuring your other clients

<Tabs>
  <Tab label="OWA webmail">
    From the OWA webmail you can decrypt received emails and verify their signature, as well as sign and encrypt the messages you send. A dedicated S/MIME menu is available in the options, under <code className="action">Mail</code> > <code className="action">S/MIME</code>.

    <img className="thumbnail" alt="S/MIME settings page in the OWA options" src="/images/web-cloud/email-and-collaborative-solutions/microsoft-exchange/feature-smime/smime-owa-settings.png" loading="lazy" />

    {/* DRAFT: Reconstructed screenshot (HTML mock), not a capture of a live OWA: replace with a real screenshot from the product team. */}

    Reading an encrypted email or verifying a signature requires a **browser compatible with the S/MIME control**, with the extension installed and configured. When the operation is not possible, OWA displays an error message from which the extension can be downloaded directly.

    <img className="thumbnail" alt="OWA warning that the digital signature was not verified because the S/MIME control is not installed" src="/images/web-cloud/email-and-collaborative-solutions/microsoft-exchange/feature-smime/smime-owa-control-missing.png" loading="lazy" />

    {/* DRAFT: Reconstructed screenshot (HTML mock), not a capture of a live OWA: replace with a real screenshot from the product team. */}

    Once the extension is installed, open its settings and add the URL of the Exchange webmail hosting your mailbox to the list of trusted domains, then save.

    <img className="thumbnail" alt="Microsoft S/MIME extension options with the Exchange webmail domain added to the trusted domains" src="/images/web-cloud/email-and-collaborative-solutions/microsoft-exchange/feature-smime/smime-owa-extension-domains.png" loading="lazy" />

    {/* DRAFT: Reconstructed screenshot (HTML mock), not a capture of a live OWA: replace with a real screenshot from the product team. */}

    :::warning
    **The extension does not work in the reading pane.** Open the email in a dedicated window to use the S/MIME functions.

    :::
  </Tab>
  <Tab label="iOS and Android">
    On a mobile device, the email application must support S/MIME for on-premise Exchange.

    The simplest way to install the certificate on the phone is to **send it by email to the user**: they then tap the attachment containing the certificate to start its installation, and follow the prompts of the operating system.

    {/* DRAFT: To document with the product team — which mobile applications are known to support S/MIME for on-premise Exchange (native iOS Mail, Outlook mobile, Gmail?), and the sign/encrypt steps once the certificate is installed. The source draft covers the certificate installation only. */}
  </Tab>
</Tabs>

## How it works

### How encryption works <a name="how-encryption-works"></a>

Each user has a certificate containing a public key and a private key. When an encrypted email is sent:

1. The email client retrieves the recipient's certificate, and therefore their public key.
2. The message is encrypted with this public key.
3. Once encrypted, the message is unreadable to any intermediary, including the Exchange server.
4. Only the recipient can decrypt it, with their private key stored on their device.

This is why you must already have the recipient's certificate to encrypt an email addressed to them.

<img className="thumbnail" alt="Diagram of S/MIME encryption: the sender gets the recipient public key from the Global Address List, encrypts the email, and only the recipient private key can decrypt it" src="/images/assets/schemas/emails/smime-encryption-diagram.png" loading="lazy" />

### How the signature works

1. The sender uses their private key to sign the message.
2. The email client computes a fingerprint (hash) of the email content.
3. This fingerprint is encrypted with the private key: this forms the signature.
4. The recipient's email client verifies the signature with the sender's public key, retrieved from their certificate published in the Exchange organisation.

Any modification of the message invalidates the signature.

<img className="thumbnail" alt="Diagram of the S/MIME signature: the sender signs a fingerprint of the message with their private key, the recipient verifies it with the sender public key, and any modification invalidates the signature" src="/images/assets/schemas/emails/smime-signature-diagram.png" loading="lazy" />

## Limitations

- To encrypt an email, you must already have the recipient's certificate.
- Losing the device or the certificates means **losing access to encrypted emails** if no backup exists. Keep a backup copy of your certificate in a safe place.
- S/MIME is less suited to multi-client, web and mobile use without specific configuration on each client.
- If a transport rule modifies the body of the email, the signed email is shown as an attachment in the recipient's Outlook.

## Go further <a name="go-further"></a>

[Exchange - Configure your email account on Outlook for Windows](/guides/web-cloud/email-and-collaborative-solutions/microsoft-exchange/how-to-configure-outlook-2016)

[Using your email address from the Outlook Web App (OWA) webmail](/guides/web-cloud/email-and-collaborative-solutions/using-the-outlook-web-app-webmail/email-owa)

[Configuring two-factor authentication on an Exchange account](/guides/web-cloud/email-and-collaborative-solutions/microsoft-exchange/manage-2fa-exchange)

Join our [community of users](/links/community).
