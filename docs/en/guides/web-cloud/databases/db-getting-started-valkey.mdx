---
title: Getting started with Valkey on Web Cloud Databases
description: Find out how to authorise your IP addresses, retrieve the connection parameters of your Valkey instance and test it from the command line
lastUpdated: 2026-09-17
---

import { Tab, Tabs } from '@rspress/core/theme';

## Objective

**Valkey** is an open source **in-memory** data store, compatible with the clients and protocols of the Redis ecosystem (RESP, REdis Serialization Protocol). It is suited to high-performance **caches**, **sessions**, **lightweight queues** and **counters**.

**Find out how to connect to your Valkey instance on Web Cloud Databases: authorising IP addresses, retrieving the connection parameters and testing from a command-line client.**

:::warning
Secure access to your instance (authorised IPs, strong passwords) and test in a pre-production environment before going live.

:::

[[fragment:support-scope]]

## Requirements

- A [Valkey instance on Web Cloud Databases](/links/web/databases) (included with a Performance, Agency, Agency Plus or Agency Max [Web Hosting](/links/web/hosting) plan).

[[cpnav:web-cloud-databases]]

## Instructions

### Part 1 — Authorise the access IP address

By default, your Valkey instance refuses public connections. To authorise (whitelist) the IP address you are connecting from, refer to our guide "[Web Cloud Databases - How to authorise an IP address?](/guides/web-cloud/databases/db-authorise-ip-mask)"

Repeat the operation for every machine that needs to access your Valkey instance.

### Part 2 — Prepare the connection parameters <a id="get-credentials"></a>

#### Retrieve the connection parameters

Click on the tabs below to view each of the **2** steps.

<Tabs>
  <Tab label="Step 1">
    In the list of your services, select the relevant solution.

    <img className="thumbnail" alt="List of Web Cloud Databases services in the OVHcloud Control Panel" src="/images/assets/screens/control-panel/product-selection/web-cloud/web-cloud-databases.png" loading="lazy" />
  </Tab>
  <Tab label="Step 2">
    In the <code className="action">General information</code> tab, note the following details:

    - **Host name**: for example `aaXXXXX-XXX.eu.clouddb.ovh.net`.
    - **Port**: for example `6379`.
  </Tab>
</Tabs>

:::info
OVHcloud does not offer a TLS-encrypted port on Redis and Valkey database servers. Communications must use the default, unencrypted port only.

:::

#### Set the password of the `default` user

Your Valkey instance comes with a `default` user, the only account available: use it to connect.

:::warning
Its password is never displayed in the OVHcloud Control Panel. You must change it at least once to know it.

:::

Click on the tabs below to view each of the **4** steps.

<Tabs>
  <Tab label="Step 1">
    In the list of your services, select the relevant solution.

    <img className="thumbnail" alt="List of Web Cloud Databases services in the OVHcloud Control Panel" src="/images/assets/screens/control-panel/product-selection/web-cloud/web-cloud-databases.png" loading="lazy" />
  </Tab>
  <Tab label="Step 2">
    On the page that appears, click the <code className="action">Users and rights</code> tab.

    <img className="thumbnail" alt="Users and rights tab of a Web Cloud Databases instance" src="/images/assets/screens/control-panel/product-selection/web-cloud/web-cloud-databases/users-and-rights.png" loading="lazy" />
  </Tab>
  <Tab label="Step 3">
    Click the <code className="action">...</code> button to the right of the `default` user, then click <code className="action">Change password</code>.

    <img className="thumbnail" alt="Change password menu to the right of a user in the Users and rights tab" src="/images/assets/screens/control-panel/product-selection/web-cloud/web-cloud-databases/users-and-rights/wcdb-change-password.png" loading="lazy" />
  </Tab>
  <Tab label="Step 4">
    Enter a strong password, confirm it, then click <code className="action">Confirm</code>. Wait a few minutes for the change to take effect before testing the connection.

    <img className="thumbnail" alt="Password change window with the criteria to be met" src="/images/assets/screens/control-panel/product-selection/web-cloud/web-cloud-databases/users-and-rights/wcdb-change-password-window.png" loading="lazy" />
  </Tab>
</Tabs>

#### Make a note of your details

Keep the following details:

- Host name
- Port
- Password of the `default` user

These are required to test the connection to your instance from a Valkey client or your application.

### Part 3 — Test the connection with a command-line client

To check that your Valkey instance is working, connect to it for the first time:

#### Install the connection client

In this example, we are using the `redis-cli` client, which is compatible with Valkey and easy to install. You can use the client of your choice. Open a terminal and run the command below for your operating system.

<Tabs>
  <Tab label="Debian/Ubuntu">
    ```bash
    sudo apt update && sudo apt -y install redis-tools
    ```
  </Tab>
  <Tab label="AlmaLinux/Rocky">
    ```bash
    sudo dnf -y install redis
    ```
  </Tab>
  <Tab label="macOS">
    Install [Homebrew](https://brew.sh/) by following the instructions on its website, then install `redis-cli`:

    ```bash
    brew install redis
    ```
  </Tab>
  <Tab label="Windows">
    In the Start menu, type `PowerShell`, right-click the result, then click <code className="action">Run as administrator</code>.

    Install [Chocolatey](https://chocolatey.org/install) by running the command below:

    ```powershell
    Set-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))
    ```

    Install `redis-cli`:

    ```powershell
    choco install redis-64 -y
    ```
  </Tab>
</Tabs>

Once you have installed the client, check that it is working by running the command below:

```bash
redis-cli --version
```

#### Connect to the instance

In your terminal, run the command below:

```bash
redis-cli -h <hostname> -p 6379
```

Replace `<hostname>` with your hostname, and `6379` with your port if it differs. To retrieve these values, refer to the "[Part 2 — Prepare the connection parameters](#get-credentials)" section.

#### Check that everything works

If the connection succeeds, the command prompt of your instance appears. Authenticate with the `AUTH` command, replacing `<password>` with the password of the `default` user:

```console
aaXXXXX-XXX.eu.clouddb.ovh.net:6379> AUTH <password>
OK
```

Then check that the server responds:

```console
aaXXXXX-XXX.eu.clouddb.ovh.net:6379> PING
```

The database should reply:

```console
PONG
```

Also try:

```console
aaXXXXX-XXX.eu.clouddb.ovh.net:6379> SET ovh-test-connexion "hello"
aaXXXXX-XXX.eu.clouddb.ovh.net:6379> GET ovh-test-connexion
```

You should see:

```console
"hello"
```

Delete the test key:

```console
aaXXXXX-XXX.eu.clouddb.ovh.net:6379> DEL ovh-test-connexion
```

To quit, type:

```console
aaXXXXX-XXX.eu.clouddb.ovh.net:6379> QUIT
```

If the tests above work, your Valkey instance is operational.

## Go further

For specialised services (SEO, development, etc.), contact [OVHcloud partners](/links/partner).

If you would like assistance using and configuring your OVHcloud solutions, please refer to our [support offers](/links/support).

Join our [community of users](/links/community).
