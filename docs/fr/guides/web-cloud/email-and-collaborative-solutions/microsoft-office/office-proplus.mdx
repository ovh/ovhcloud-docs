---
title: Utiliser le bureau à distance avec Microsoft 365 apps
description: Découvrez comment installer et utiliser Microsoft 365 apps sur un bureau distant (RDS) ou un ordinateur partagé
lastUpdated: 2026-09-16
availableIn: [eu]
---

## Objectif

Vous souhaitez utiliser la suite logicielle présente dans le pack Microsoft 365 apps sur une machine distante ou partagée. Pour ce faire, il est nécessaire de suivre la procédure d'installation décrite dans ce guide.

**Découvrez comment installer et utiliser Microsoft 365 apps sur un bureau distant (RDS) ou un ordinateur partagé.**

## Prérequis

- Disposer d'une licence Microsoft 365 apps for entreprise (Anciennement Office 365 ProPlus)
- Utiliser Microsoft Windows via un bureau à distance (**R**emote **D**esktop **S**ervices)
- Utiliser une version de Windows Server sur laquelle Microsoft prend en charge Microsoft 365 Apps (voir la note ci-dessous)

:::warning
Microsoft ne prend pas en charge Microsoft 365 Apps sur Windows Server 2008 R2, Windows Server 2012 ni Windows Server 2012 R2. La prise en charge de Windows Server 2016 et 2019 a pris fin en octobre 2025, et celle de Windows Server 2022 prend fin en octobre 2026.

Consultez la [matrice de compatibilité Microsoft](https://learn.microsoft.com/fr-fr/microsoft-365-apps/deploy/deploy-microsoft-365-apps-remote-desktop-services) avant de commencer, afin de ne pas déployer sur une version qui n'est plus prise en charge.
:::

:::warning
Microsoft 365 Apps for business ne prend pas en charge l'activation sur ordinateur partagé. Celle-ci est en revanche incluse avec **Microsoft 365 Business Premium**.
:::

## En pratique

Ce guide est basé sur les informations délivrées dans le guide Microsoft [Déployer Microsoft 365 Apps à l’aide des services Bureau à distance](https://learn.microsoft.com/fr-fr/microsoft-365-apps/deploy/deploy-microsoft-365-apps-remote-desktop-services).

[[fragment:support-scope]]

:::warning
Nous vous recommandons de faire appel à un [partenaire spécialisé](https://marketplace.ovhcloud.com/products) et/ou de contacter l'éditeur du service si vous éprouvez des difficultés.
:::

### Méthode 1 : Configuration manuelle

L'installation d'une offre Microsoft 365 Apps for entreprise sur un ordinateur partagé en utilisant les services Bureau à distance (RDS) ne fonctionne pas sans une configuration spécifique. Sans ce paramétrage, vous devriez obtenir le message suivant :

:::info
Si vous avez déjà réalisé l'installation de votre licence Office 365 Proplus, il est nécessaire de la **désinstaller**.
:::

- Une fois votre licence désinstallée, veuillez [cliquer ici](https://www.microsoft.com/en-us/download/details.aspx?id=49117) afin de télécharger l'outil de déploiement Microsoft 365.

- **Exécutez** l'outil de déploiement sur votre machine.

- Modifiez le fichier `configuration.xml`.

Éditez le fichier `configuration.xml`, puis décommentez les lignes suivantes :

```xml
<Display Level="None" AcceptEULA="True" />
<Property Name="SharedComputerLicensing" Value="1" />
```

Si ces lignes n'existent pas, vous pouvez les copier/coller dans le fichier.

- Enregistrez les modifications effectuées. Ouvrez ensuite un terminal powershell et exécutez ces deux commandes depuis le dossier où se trouve le fichier `configuration.xml`:

```powershell
./setup.exe /download configuration.xml
```

puis

```powershell
./setup.exe /configure configuration.xml
```

:::info
L'exécution de ces commandes peut prendre plusieurs minutes.
:::

- Ouvrez l'éditeur du registre de windows en exécutant `Regedit`, puis suivre le chemin suivant :

```powershell
HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Office\ClickToRun\Configuration
```

- Vérifiez la clé suivante :

```powershell
SharedComputerLicensing
```
Assurez-vous que sa valeur est à `1`. Si cette clé n'existe pas, vous pouvez la créer.

- Lancez une application de la suite Office 365, vous serez alors invité à saisir votre nom d'utilisateur et mot de passe.

- Vous pouvez maintenant utiliser votre suite Office 365 depuis votre ordinateur partagé en utilisant les services Bureau à distance (RDS).

## Aller plus loin

Échangez avec notre [communauté d'utilisateurs](/links/community).
