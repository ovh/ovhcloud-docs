---
title: Démarrez avec Valkey sur Web Cloud Databases
description: Découvrez comment autoriser vos adresses IP, récupérer les paramètres de connexion de votre instance Valkey et la tester en ligne de commande
lastUpdated: 2026-09-18
---

import { Tab, Tabs } from '@rspress/core/theme';

## Objectif

**Valkey** est un magasin de données **en mémoire**, open source, compatible avec les clients et les protocoles de l’écosystème Redis (RESP, REdis Serialization Protocol). Il convient aux traitements à haute performance : **caches**, **sessions**, **files d’attente légères** et **compteurs**.

**Découvrez comment vous connecter à votre instance Valkey sur Web Cloud Databases : autorisation des adresses IP, récupération des paramètres de connexion et test depuis un client en ligne de commande.**

:::warning
Sécurisez l’accès à votre instance (IP autorisées, mots de passe robustes) et testez en pré-production avant la mise en ligne.

:::

[[fragment:support-scope]]

## Prérequis

- Disposer d’une [instance Valkey sur Web Cloud Databases](/links/web/databases) (incluse dans une offre d’[hébergement web](/links/web/hosting) Performance, Agency, Agency Plus ou Agency Max).

[[cpnav:web-cloud-databases]]

## En pratique

### Partie 1 — Autoriser l’adresse IP d’accès

Par défaut, votre instance Valkey refuse les connexions publiques. Pour autoriser (mettre en liste blanche) l’adresse IP depuis laquelle vous vous connectez, consultez notre guide « [Web Cloud Databases - Comment autoriser une adresse IP ?](/guides/web-cloud/databases/db-authorise-ip-mask) »

Répétez l’opération pour chaque machine qui doit accéder à votre instance Valkey.

### Partie 2 — Préparer les paramètres de connexion <a id="get-credentials"></a>

#### Récupérer les paramètres de connexion

Cliquez sur les onglets ci-dessous pour afficher successivement chacune des **2** étapes.

<Tabs>
  <Tab label="Étape 1">
    Dans la liste de vos services, choisissez la solution concernée.

    <img className="thumbnail" alt="Liste des services Web Cloud Databases dans l’espace client OVHcloud" src="/images/assets/screens/control-panel/product-selection/web-cloud/web-cloud-databases.png" loading="lazy" />
  </Tab>
  <Tab label="Étape 2">
    Dans l’onglet <code className="action">Informations générales</code>, notez les informations suivantes :

    - **Nom d’hôte** : par exemple `aaXXXXX-XXX.eu.clouddb.ovh.net`.
    - **Port** : par exemple `6379`.
  </Tab>
</Tabs>

:::info
OVHcloud ne propose pas de port chiffré via TLS sur les serveurs de bases de données Redis et Valkey. Les communications doivent passer uniquement par le port par défaut, non chiffré.

:::

#### Définir le mot de passe de l’utilisateur `default`

Votre instance Valkey est livrée avec un utilisateur `default`, seul compte disponible : utilisez-le pour vous connecter.

:::warning
Son mot de passe n’est jamais affiché dans l’espace client OVHcloud. Vous devez le changer au moins une fois pour le connaître.

:::

Cliquez sur les onglets ci-dessous pour afficher successivement chacune des **4** étapes.

<Tabs>
  <Tab label="Étape 1">
    Dans la liste de vos services, choisissez la solution concernée.

    <img className="thumbnail" alt="Liste des services Web Cloud Databases dans l’espace client OVHcloud" src="/images/assets/screens/control-panel/product-selection/web-cloud/web-cloud-databases.png" loading="lazy" />
  </Tab>
  <Tab label="Étape 2">
    Sur la page qui s’affiche, cliquez sur l’onglet <code className="action">Utilisateurs et droits</code>.

    <img className="thumbnail" alt="Onglet Utilisateurs et droits d’une instance Web Cloud Databases" src="/images/assets/screens/control-panel/product-selection/web-cloud/web-cloud-databases/users-and-rights.png" loading="lazy" />
  </Tab>
  <Tab label="Étape 3">
    Cliquez sur le bouton <code className="action">...</code> à droite de l’utilisateur `default`, puis sur <code className="action">Changer le mot de passe</code>.

    <img className="thumbnail" alt="Menu Changer le mot de passe à droite d’un utilisateur dans l’onglet Utilisateurs et droits" src="/images/assets/screens/control-panel/product-selection/web-cloud/web-cloud-databases/users-and-rights/wcdb-change-password.png" loading="lazy" />
  </Tab>
  <Tab label="Étape 4">
    Saisissez un mot de passe robuste, confirmez-le, puis cliquez sur <code className="action">Valider</code>. Attendez quelques minutes que la modification prenne effet avant de tester la connexion.

    <img className="thumbnail" alt="Fenêtre de changement du mot de passe avec les critères à respecter" src="/images/assets/screens/control-panel/product-selection/web-cloud/web-cloud-databases/users-and-rights/wcdb-change-password-window.png" loading="lazy" />
  </Tab>
</Tabs>

#### Mémoriser vos informations

Conservez les informations suivantes :

- Nom d’hôte
- Port
- Mot de passe de l’utilisateur `default`

Ces éléments sont nécessaires pour tester la connexion à votre instance depuis un client Valkey ou votre application.

### Partie 3 — Tester la connexion avec un client en ligne de commande

Pour vérifier que votre instance Valkey est fonctionnelle, connectez-vous-y une première fois :

#### Installer le client de connexion

Dans cet exemple, nous utilisons le client `redis-cli`, compatible avec Valkey et facile à installer. Vous pouvez utiliser le client de votre choix. Ouvrez un terminal et exécutez la commande ci-dessous selon votre système d’exploitation.

<Tabs>
  <Tab label="Debian/Ubuntu">
    ```bash
    sudo apt update && sudo apt -y install redis-tools
    ```
  </Tab>
  <Tab label="AlmaLinux/Rocky">
    ```bash
    sudo dnf -y install redis
    ```
  </Tab>
  <Tab label="macOS">
    Installez [Homebrew](https://brew.sh/fr/) en suivant les instructions de son site, puis installez `redis-cli` :

    ```bash
    brew install redis
    ```
  </Tab>
  <Tab label="Windows">
    Dans le menu Démarrer, saisissez `PowerShell`, faites un clic droit sur le résultat, puis cliquez sur <code className="action">Exécuter en tant qu’administrateur</code>.

    Installez [Chocolatey](https://chocolatey.org/install) en exécutant la commande ci-dessous :

    ```powershell
    Set-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))
    ```

    Installez `redis-cli` :

    ```powershell
    choco install redis-64 -y
    ```
  </Tab>
</Tabs>

Une fois le client installé, vérifiez qu’il fonctionne en exécutant la commande ci-dessous :

```bash
redis-cli --version
```

#### Se connecter à l’instance

Dans votre terminal, exécutez la commande ci-dessous :

```bash
redis-cli -h <hostname> -p 6379
```

Remplacez `<hostname>` par votre nom d’hôte, et `6379` par votre port s’il diffère. Pour récupérer ces valeurs, reportez-vous à la section « [Partie 2 — Préparer les paramètres de connexion](#get-credentials) ».

#### Vérifier que tout fonctionne

Si la connexion réussit, l’invite de commande de votre instance s’affiche. Authentifiez-vous avec la commande `AUTH`, en remplaçant `<password>` par le mot de passe de l’utilisateur `default` :

```console
aaXXXXX-XXX.eu.clouddb.ovh.net:6379> AUTH <password>
OK
```

Vérifiez ensuite que le serveur répond :

```console
aaXXXXX-XXX.eu.clouddb.ovh.net:6379> PING
```

La base de données doit répondre :

```console
PONG
```

Essayez aussi :

```console
aaXXXXX-XXX.eu.clouddb.ovh.net:6379> SET ovh-test-connexion "hello"
aaXXXXX-XXX.eu.clouddb.ovh.net:6379> GET ovh-test-connexion
```

Vous devez voir :

```console
"hello"
```

Supprimez la clé de test :

```console
aaXXXXX-XXX.eu.clouddb.ovh.net:6379> DEL ovh-test-connexion
```

Pour quitter, tapez :

```console
aaXXXXX-XXX.eu.clouddb.ovh.net:6379> QUIT
```

Si les tests précédents fonctionnent, votre instance Valkey est opérationnelle.

## Aller plus loin

Pour des prestations spécialisées (référencement, développement, etc.), contactez les [partenaires OVHcloud](/links/partner).

Si vous souhaitez bénéficier d’une assistance à l’usage et à la configuration de vos solutions OVHcloud, nous vous proposons de consulter nos différentes [offres de support](/links/support).

Échangez avec notre [communauté d’utilisateurs](/links/community).
