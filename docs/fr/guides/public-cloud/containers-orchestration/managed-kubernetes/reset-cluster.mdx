---
title: Réinitialiser un cluster OVHcloud Managed Kubernetes
description: Découvrez comment réinitialiser un cluster OVHcloud Managed Kubernetes
lastUpdated: 2026-02-25
---

import Api from '@components/Api';




## Objectif

Le service OVHcloud Managed Kubernetes vous fournit des clusters Kubernetes sans que vous ayez à vous soucier de leur installation ou de leur exploitation.

Depuis l'espace client OVHcloud et via l'API, vous pouvez réinitialiser votre cluster OVHcloud Managed Kubernetes.

:::info
Lors d'une réinitialisation, toutes les données de l'ETCD seront supprimées (pods, déploiements, services, secrets, CRD, etc.). De la même manière, les load balancers ainsi que tous les PVC (Persistent Volume Claim) avec leurs volumes PCI cinder seront supprimés.
Les nœuds seront soit supprimés, soit réinstallés (selon l'option choisie lors de la réinitialisation).(selon l'option choisie lors de la réinitialisation).

:::


L'ensemble de la configuration du cluster est réinitialisé, mais certaines valeurs peuvent être définies (facultatif) :

- workerNodesPolicy : reinstall|delete
- minor_version : par ex. 1.25
- updatePolicy : "ALWAYS_UPDATE" par défaut
- customization : configuration de l'API Server et du kube proxy
- privateNetworkId : UUID OpenStack du réseau privé
- privateNetworkConfiguration : true|false
- defaultVrackGateway : IP de la gateway
- kubeProxyMode : ipvs|iptables

## Prérequis

- un cluster OVHcloud Managed Kubernetes

{/* CP-NAV-START:publiccloud-projects */}
---

### Accès à l'espace client OVHcloud

- **Lien direct :** <ManagerLink to="/#/pci/projects">Public Cloud Projects</ManagerLink>
- **Pour accéder à vos services :** <code className="action">Public Cloud</code> > sélectionnez votre projet

---
{/* CP-NAV-END:publiccloud-projects */}

## En pratique

### Réinitialiser un cluster Kubernetes depuis l'espace client OVHcloud

#### Étape 1 - Demander la réinitialisation du cluster depuis l'espace client OVHcloud

Accédez à l'interface d'administration de vos clusters OVHcloud Managed Kubernetes en cliquant sur <code className="action">Managed Kubernetes Service</code> dans le menu de gauche.

<img className="thumbnail" alt="Lister vos clusters Kubernetes" src="/images/public-cloud/containers-orchestration/managed-kubernetes/resetting-a-cluster/clusters.png" loading="lazy" />

Cliquez sur votre cluster Kubernetes.

<img className="thumbnail" alt="Accéder à l'interface d'administration" src="/images/public-cloud/containers-orchestration/managed-kubernetes/resetting-a-cluster/resetting_a_cluster-01.png" loading="lazy" />

Dans l'onglet *Service* de l'interface d'administration, cliquez sur *Réinitialiser votre cluster*.

#### Étape 2 - Choisir le type de réinitialisation souhaité

Vous disposez de deux options dans le menu de réinitialisation, *Supprimer* et *Réinstaller*.

- Choisir *Supprimer les nœuds* signifie que les nœuds actuels du cluster sont supprimés, et que le cluster est réinitialisé à un état vide.

- Choisir *Conserver et réinstaller les nœuds* signifie que le cluster est réinitialisé sans détruire les nœuds.

<img className="thumbnail" alt="Choisir le type de réinitialisation souhaité" src="/images/public-cloud/containers-orchestration/managed-kubernetes/resetting-a-cluster/resetting_a_cluster-03.png" loading="lazy" />

Vous pouvez également choisir la version mineure du cluster et le réseau privé associé.

Cliquez sur le bouton <code className="action">Confirmer</code> pour continuer.

#### Étape 3 - Attendre la fin de la réinitialisation

Selon le type de réinitialisation choisi, le processus peut prendre plusieurs minutes. Pendant ce temps, un message s'affiche sur le manager pour vous avertir que le cluster est en cours de réinitialisation :

<img className="thumbnail" alt="réinitialisation en cours" src="/images/public-cloud/containers-orchestration/managed-kubernetes/resetting-a-cluster/resetting_a_cluster-04.png" loading="lazy" />

### Réinitialiser un cluster Kubernetes via l'API

#### L'API Explorer

Pour simplifier les choses, nous utilisons l'[API Explorer](/guides/manage-and-operate/api/first-steps), qui permet d'explorer, d'apprendre et d'interagir avec l'API de manière interactive.

Si vous vous rendez dans la section Kubernetes de l'API Explorer, vous verrez les endpoints disponibles, tels que :

<Api version="v1" section="/cloud" method="GET" route={"/cloud/project/{serviceName}/kube"} />

#### Endpoints de l'API

- Réinitialiser un cluster Kubernetes :

<Api version="v1" section="/cloud" method="POST" route={"/cloud/project/\{serviceName\}/kube/\{kubeId\}/reset"} />

**Entrée :**
```json
{
  "name": "my-test-cluster",
  "updatePolicy": "ALWAYS_UPDATE",
  "version": "1.34"
}
```

**Résultat :**
```json
null
```

Par défaut, si vous ne le spécifiez pas, l'option `workerNodesPolicy` sera équivalente à `delete`. Si vous ne souhaitez pas que vos nœuds soient supprimés mais réinstallés à la place, vous devez spécifier `"workerNodesPolicy": "reinstall"`.

:::info
Vous devriez recevoir une réponse avec un contenu `null`. Ce message sera amélioré à l'avenir avec les mêmes informations que celles obtenues après la création d'un cluster Kubernetes.

:::


- Vérifier que le cluster Kubernetes est en cours de réinitialisation :

<Api version="v1" section="/cloud" method="GET" route={"/cloud/project/\{serviceName\}/kube/\{kubeId\}"} />

**Résultat :**
```json
{
  "id": "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxx",
  "region": "GRA5",
  "name": "my-test-cluster",
  "url": "xxxxxx.xx.gra.k8s.ovh.net",
  "nodesUrl": "xxxxxx.nodes.c1.gra.k8s.ovh.net",
  "version": "1.34.1",
  "nextUpgradeVersions": [],
  "kubeProxyMode": "iptables",
  "customization": {
    "apiServer": {
      "admissionPlugins": {
        "enabled": [
          "AlwaysPullImages",
          "NodeRestriction"
        ],
        "disabled": []
      }
    }
  },
  "status": "REDEPLOYING",
  "updatePolicy": "ALWAYS_UPDATE",
  "isUpToDate": true,
  "controlPlaneIsUpToDate": true,
  "privateNetworkId": null,
  "createdAt": "2023-03-21T10:53:35Z",
  "updatedAt": "2023-03-22T09:16:34Z"
}
```

## Aller plus loin

Pour obtenir une vue d'ensemble du service OVHcloud Managed Kubernetes, consultez la [page OVHcloud Managed Kubernetes](/links/public-cloud/kubernetes).

Sinon, pour passer directement à l'utilisation pratique de votre cluster Kubernetes, nous vous invitons à consulter nos [tutoriels](/guides/public-cloud/containers-orchestration/managed-kubernetes/overview).

- Si vous avez besoin d'une formation ou d'une assistance technique pour la mise en oeuvre de nos solutions, contactez votre commercial ou cliquez sur [ce lien](/links/professional-services) pour obtenir un devis et demander une analyse personnalisée de votre projet à nos experts de l’équipe Professional Services.

- Échangez avec notre [communauté d'utilisateurs](/links/community).
