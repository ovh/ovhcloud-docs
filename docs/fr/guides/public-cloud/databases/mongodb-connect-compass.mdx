---
title: Se connecter depuis Compass au service Public Cloud Databases pour MongoDB
description: Connectez-vous à votre service Public Cloud Databases pour MongoDB à l'aide de MongoDB Compass
lastUpdated: 2025-01-06
---

## Objectif

Public Cloud Databases vous permet de vous concentrer sur la création et le déploiement de vos applications cloud, pendant qu'OVHcloud prend en charge l'infrastructure de base de données et son maintien en conditions opérationnelles.

**Ce guide explique comment se connecter à une instance de base de données MongoDB avec l'un des outils de gestion Open Source (SSPL) les plus connus au monde pour MongoDB : MongoDB Compass.**

## Prérequis

- Un [projet Public Cloud](/links/public-cloud/public-cloud) dans votre compte OVHcloud
- Une base de données MongoDB fonctionnant sur votre service Public Cloud Databases OVHcloud ([ce guide](/guides/public-cloud/databases/getting-started) peut vous aider à répondre à ce prérequis)
- [Configurer votre instance MongoDB](/guides/public-cloud/databases/mongodb-manage-control-panel) pour accepter les connexions entrantes
- Une version stable de MongoDB Compass installée ainsi qu'une connectivité réseau publique (Internet). Ce guide a été réalisé avec MongoDB Compass version 1.30.1.

## Concept

Une instance MongoDB peut être gérée de plusieurs façons.
L'une des plus simples, mais aussi des plus puissantes, consiste à utiliser une interface en ligne de commande (CLI), comme indiqué dans notre guide : [Se connecter à MongoDB avec la CLI](/guides/public-cloud/databases/mongodb-connect-cli), ou à utiliser des langages de programmation tels que [PHP](/guides/public-cloud/databases/mongodb-connect-php) ou [Python](/guides/public-cloud/databases/mongodb-connect-python).

Une autre méthode consiste à interagir directement à l'aide d'un outil de gestion pour MongoDB : MongoDB Compass.

Pour cela, nous devrons installer MongoDB Compass, puis configurer notre instance Public Cloud Databases pour MongoDB afin d'accepter les connexions entrantes, et enfin configurer MongoDB.

## En pratique

### Installation

Veuillez suivre la [documentation officielle](https://docs.mongodb.com/compass/current/install/) pour installer MongoDB Compass.

Nous sommes maintenant prêts à découvrir comment nous connecter à notre instance MongoDB.

### Se connecter avec MongoDB Compass

Dans MongoDB Compass, renseignez le champ de connexion avec l'`URI de service` :

<img className="thumbnail" alt="Nouvelle connexion" src="/images/public-cloud/databases/mongodb-07-connect-compass/new-connection.png" loading="lazy" />

Vous pouvez maintenant interagir avec votre service Public Cloud Databases pour MongoDB :

<img className="thumbnail" alt="Connecté" src="/images/public-cloud/databases/mongodb-07-connect-compass/connected.png" loading="lazy" />

### Insérer et interroger des données

Vous pouvez utiliser le [mongoshell](https://www.mongodb.com/docs/mongodb-shell/), intégré à Compass, pour créer votre première base de données et collection. Voici un script qui crée la base de données **company** et la collection **customer**, puis insère 100 documents aléatoires.

#### Charger des données dans MongoDB

Pour charger 100 documents dans une collection nommée `customer` avec des données aléatoires, utilisez le script `mongosh` suivant :

```javascript
use company;

// Function to generate random data
function getRandomData() {
    const firstNames = ["John", "Jane", "Mary", "Michael", "Sarah", "Robert", "Linda", "James", "Patricia", "David"];
    const lastNames = ["Smith", "Johnson", "Williams", "Jones", "Brown", "Davis", "Miller", "Wilson", "Moore", "Taylor"];
    const domains = ["example.com", "email.com", "mail.com", "test.com", "demo.com"];
    
    function getRandomItem(array) {
        return array[Math.floor(Math.random() * array.length)];
    }
    
    return {
        firstName: getRandomItem(firstNames),
        lastName: getRandomItem(lastNames),
        email: `${getRandomItem(firstNames).toLowerCase()}.${getRandomItem(lastNames).toLowerCase()}@${getRandomItem(domains)}`,
        age: Math.floor(Math.random() * 60) + 18,
        createdAt: new Date()
    };
}

// Insert 100 random documents into the customer collection
const bulk = db.customer.initializeUnorderedBulkOp();
for (let i = 0; i < 100; i++) {
    bulk.insert(getRandomData());
}
bulk.execute();

print("100 random documents inserted into the 'customer' collection.");
```
<img className="thumbnail" alt="Shell Compass" src="/images/public-cloud/databases/mongodb-07-connect-compass/compassShell.png" loading="lazy" />

#### Interroger des données avec le framework d'agrégation

Le pipeline d'agrégation MongoDB ci-dessous utilise le [framework d'agrégation MongoDB](https://www.mongodb.com/docs/manual/aggregation/) pour regrouper les clients par âge et compter chaque occurrence. Vous pouvez utiliser le mongoshell pour l'exécuter :

```javascript
db.customer.aggregate([
    {
        $group: {
            _id: "$age",
            count: { $sum: 1 }
        }
    },
    {
        $sort: { _id: 1 }
    }
]);
```

Vous pouvez également utiliser l'interface de Compass pour exécuter le pipeline d'agrégation.

<img className="thumbnail" alt="Agrégation Compass" src="/images/public-cloud/databases/mongodb-07-connect-compass/compassAggregation.png" loading="lazy" />

## Aller plus loin

Consultez la [documentation](https://docs.mongodb.com/compass/current/) pour découvrir toutes les fonctionnalités et comment interagir avec vos données.

[Capacités de MongoDB](/guides/public-cloud/databases/mongodb-concept-capabilities)

[Configurer le vRack pour Public Cloud](/guides/public-cloud/network-services/vrack)

Consultez le [dépôt d'exemples GitHub](https://github.com/ovh/public-cloud-databases-examples/tree/main/databases/mongodb) pour découvrir comment vous connecter à votre base de données avec plusieurs langages.

Rejoignez notre [communauté d'utilisateurs](/links/community).

## Nous voulons vos retours !

Nous serions ravis de répondre à vos questions et apprécions tout retour que vous pourriez nous faire.

Si vous avez besoin d'une formation ou d'une assistance technique pour la mise en oeuvre de nos solutions, contactez votre commercial ou cliquez sur [ce lien](/links/professional-services) pour obtenir un devis et demander une analyse personnalisée de votre projet à nos experts de l’équipe Professional Services.
Vous êtes sur Discord ? Rejoignez notre chaîne via [https://discord.gg/ovhcloud](https://discord.gg/ovhcloud) et interagissez directement avec l’équipe qui développe notre service de bases de données !
