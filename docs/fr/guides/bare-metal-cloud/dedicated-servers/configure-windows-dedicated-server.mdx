---
title: Installer et configurer Windows Server sur un serveur dédié
description: Installez Windows Server sur un serveur dédié OVHcloud et configurez-le pour l'utiliser avec une Additional IP, le vRack, l'IPv6 et le Backup Storage.
lastUpdated: 2026-06-17
---

## Objectif

Windows Server est un système d'exploitation pris en charge sur les serveurs dédiés OVHcloud. Une fois installé, vous devez configurer le système Windows afin qu'il puisse utiliser les services réseau et de stockage OVHcloud rattachés au serveur.

Ce guide vous accompagne pour installer Windows Server depuis l'espace client OVHcloud, vous connecter au serveur, puis effectuer la configuration côté Windows nécessaire pour interagir avec les principaux services OVHcloud rattachés à un serveur dédié.

**Dans ce guide, vous allez apprendre à :**

- Installer Windows Server depuis l'espace client
- Vous connecter au serveur avec le Bureau à distance et effectuer la configuration initiale
- Configurer Windows pour utiliser une Additional IP
- Configurer Windows pour utiliser le réseau privé vRack
- Configurer Windows pour utiliser le bloc IPv6 fourni avec le serveur
- Accéder au Backup Storage OVHcloud depuis Windows

:::warning
La réinstallation d'un système d'exploitation efface toutes les données présentes sur les disques du serveur. Sauvegardez les données que vous souhaitez conserver avant de commencer.
:::

## Prérequis

- Un [serveur dédié](/links/bare-metal/bare-metal) dans votre compte OVHcloud
- Accès à l'<ManagerLink to="/">espace client OVHcloud</ManagerLink>
- Une licence Windows Server valide. Les licences peuvent être commandées auprès d'OVHcloud ou d'un revendeur externe — consultez le guide [Gérer vos licences OVHcloud](/guides/bare-metal-cloud/dedicated-servers/windows-key).
- Un client RDP (Remote Desktop Protocol) sur votre machine locale (le client <code className="action">Connexion Bureau à distance</code> est inclus avec Windows)

## Introduction

Un serveur dédié est un serveur physique (« bare metal ») hébergé dans un centre de données OVHcloud. OVHcloud fournit le matériel, la connexion au réseau public, un réseau privé vRack optionnel et un espace Backup Storage inclus. Vous êtes responsable de l'administration du système d'exploitation installé sur le serveur, y compris de toute la configuration réseau et de stockage dans Windows.

Le tableau ci-dessous présente les principaux services avec lesquels un serveur dédié sous Windows interagit.

| Service OVHcloud | Rôle | Où l'activer |
|---|---|---|
| Additional IP | Adresses IP publiques supplémentaires rattachées au serveur | Espace client — onglet <code className="action">Réseau</code> |
| vRack | Réseau privé de niveau 2 entre les services OVHcloud | Espace client — onglet <code className="action">Réseau</code> |
| IPv6 | Le bloc IPv6 /64 fourni avec chaque serveur | Fourni automatiquement |
| Backup Storage | Espace de sauvegarde inclus (FTP/CIFS/NFS) | Espace client — onglet <code className="action">Matériel</code> |

## En pratique

**Contenu :**

- [Étape 1 — Installer Windows Server depuis l'espace client](#install)
- [Étape 2 — Vous connecter à votre serveur](#connect)
- [Étape 3 — Effectuer la configuration initiale de Windows](#initial-config)
- [Étape 4 — Configurer Windows pour interagir avec les services OVHcloud](#services)
- [Étape 5 — Vérifier la configuration](#verify)
- [Dépannage](#troubleshooting)

### Étape 1 — Installer Windows Server depuis l'espace client <a name="install"></a>

Cette étape installe (ou réinstalle) Windows Server sur votre serveur dédié. L'installation se pilote entièrement depuis l'espace client.

Connectez-vous à l'<ManagerLink to="/">espace client OVHcloud</ManagerLink> et accédez à **Bare Metal Cloud** > **Serveurs dédiés**, puis sélectionnez votre serveur. Ouvrez l'onglet <code className="action">Système</code>.

À côté du système d'exploitation, cliquez sur <code className="action">Installer</code> (ou <code className="action">Réinstaller</code> si un système d'exploitation est déjà présent). La fenêtre <code className="action">Installation du système d'exploitation</code> s'ouvre et vous guide en quatre étapes.

:::warning
Cette action est irréversible. Toutes les données présentes sur les disques seront définitivement supprimées.
:::

1. **Système d'exploitation** — sélectionnez une catégorie Windows dans la liste <code className="action">Catégorie</code>, puis choisissez la version de Windows Server dans les listes <code className="action">Distribution</code> et <code className="action">Système d'exploitation</code>.
2. **RAID matériel** — choisissez la configuration RAID sur laquelle Windows sera installé. Pour le RAID logiciel sous Windows, consultez le guide [Configurer un RAID logiciel sur un serveur Windows](/guides/bare-metal-cloud/dedicated-servers/raid-soft-windows).
3. **Partitions** — acceptez le schéma de partitionnement par défaut, ou activez <code className="action">Personnaliser la configuration des partitions</code> pour définir votre propre disposition.
4. **Options** — vérifiez le récapitulatif et confirmez les options propres à Windows.
Cliquez sur <code className="action">Installer</code> pour lancer l'installation. Le serveur redémarre et l'installation se termine automatiquement.

:::info
Windows Server nécessite une licence, qui génère des frais supplémentaires. Vous pouvez commander une licence auprès d'OVHcloud ou fournir la vôtre. Gérez vos licences dans **Bare Metal Cloud** > **Licences**. Pour plus d'informations, consultez le guide [Gérer vos licences OVHcloud](/guides/bare-metal-cloud/dedicated-servers/windows-key).
:::

### Étape 2 — Vous connecter à votre serveur <a name="connect"></a>

Une fois l'installation terminée, OVHcloud vous envoie un e-mail contenant le nom d'utilisateur et le mot de passe administrateur de votre serveur Windows.

Sur votre machine Windows locale, ouvrez le client <code className="action">Connexion Bureau à distance</code>. Saisissez l'adresse IPv4 de votre serveur, puis votre nom d'utilisateur et votre mot de passe. Un avertissement de certificat apparaît généralement lors de la première connexion — cliquez sur <code className="action">Oui</code> pour continuer.
Vous pouvez utiliser n'importe quel client compatible RDP si votre appareil local ne fonctionne pas sous Windows.

:::tip
Si le protocole RDP est indisponible — par exemple après une erreur de configuration réseau — connectez-vous via la [console Intelligent Platform Management Interface (IPMI)](/guides/bare-metal-cloud/dedicated-servers/ipmi) depuis l'onglet <code className="action">Serveur</code> de l'espace client (la section <code className="action">Connexion au serveur</code>). La console IPMI fonctionne quel que soit l'état de connectivité du système d'exploitation.
:::

Après votre première connexion, modifiez le mot de passe administrateur. Pour la procédure, consultez le guide [Modifier le mot de passe administrateur sur Windows](/guides/bare-metal-cloud/dedicated-servers/changing-admin-password-on-windows).

### Étape 3 — Effectuer la configuration initiale de Windows <a name="initial-config"></a>

Avant de connecter le serveur aux autres services OVHcloud, effectuez la configuration de base de Windows.

- Lancez Windows Update et installez les dernières mises à jour.
- Définissez le nom d'hôte du serveur, le fuseau horaire et la disposition du clavier.
- Configurez le pare-feu Windows pour autoriser les services que vous comptez exécuter. Pour une procédure spécifique au pare-feu sous Windows, consultez le guide [Activer le pare-feu sur un serveur Windows](/guides/bare-metal-cloud/dedicated-servers/activate-port-firewall-soft-win).

:::warning
OVHcloud met à votre disposition des services dont vous êtes responsable quant à leur configuration et leur gestion. Si vous avez besoin d'une assistance spécialisée, contactez un [prestataire spécialisé](/links/partner).
:::

### Étape 4 — Configurer Windows pour interagir avec les services OVHcloud <a name="services"></a>

Cette section couvre la configuration côté Windows des services OVHcloud les plus fréquemment rattachés à un serveur dédié. Chaque sous-section est indépendante — ne suivez que celles qui correspondent à votre configuration. Dans chacune, la première partie s'effectue dans l'espace client OVHcloud et la seconde dans Windows Server.

#### 4a — Additional IP

Une Additional IP est une adresse IP publique supplémentaire routée vers votre serveur. Après l'avoir commandée, vous devez la déclarer dans Windows pour que le serveur réponde sur cette adresse.

**Dans l'espace client OVHcloud**

Commandez l'Additional IP depuis l'onglet <code className="action">Réseau</code> de votre serveur (la section <code className="action">IPs additionnelles</code>). Pour plus d'informations, consultez la page [Additional IP](/links/bare-metal/ip).

**Dans Windows Server**

1. Ouvrez <code className="action">Panneau de configuration</code> > <code className="action">Centre Réseau et partage</code> > <code className="action">Modifier les paramètres de la carte</code>, puis affichez les propriétés de votre carte réseau.
2. Sélectionnez <code className="action">Protocole Internet version 4 (TCP/IPv4)</code> > <code className="action">Propriétés</code> > <code className="action">Avancé</code>, et ajoutez l'adresse de l'Additional IP.

Sur le réseau OVHcloud, une Additional IP est routée individuellement vers le serveur. Utilisez le masque de sous-réseau et la passerelle qui correspondent à la configuration de l'IP principale de votre serveur.
:::tip
Si vous comptez attribuer l'Additional IP à une machine virtuelle plutôt qu'à l'hôte, consultez le guide [Configurer un bloc d'IP dans un vRack](/guides/bare-metal-cloud/dedicated-servers/configuring-an-ip-block-in-a-vrack).
:::

#### 4b — Réseau privé vRack

Le vRack est un réseau privé de niveau 2 qui permet à votre serveur dédié de communiquer de manière privée avec d'autres services OVHcloud (autres serveurs dédiés, instances Public Cloud, etc.).

**Dans l'espace client OVHcloud**

Dans l'onglet <code className="action">Réseau</code> de votre serveur, cliquez sur <code className="action">Attacher un vRack</code> et sélectionnez le vRack cible.
**Dans Windows Server**

1. Ouvrez les propriétés de la carte réseau **privée** (la seconde interface, dédiée au vRack).
2. Attribuez une adresse IPv4 privée statique issue de votre plan d'adressage vRack. Laissez la passerelle par défaut vide — le vRack est un réseau de niveau 2 sans passerelle fournie par OVHcloud.

Pour la procédure complète, ainsi que pour la configuration de l'OLA (agrégation de liens), consultez le guide [Configurer le vRack sur un serveur dédié](/guides/bare-metal-cloud/dedicated-servers/vrack-configuring-on-dedicated-server).

#### 4c — IPv6

Chaque serveur dédié OVHcloud est livré avec un bloc IPv6 /64. L'adresse n'est pas configurée automatiquement dans Windows et doit être ajoutée manuellement.

**Dans l'espace client OVHcloud**

Récupérez votre adresse IPv6, le préfixe et la passerelle depuis l'onglet <code className="action">Réseau</code> de votre serveur (la section <code className="action">Adresses IP</code> affiche <code className="action">IPv6</code> et <code className="action">Gateway IPv6</code>).

**Dans Windows Server**

1. Ouvrez les propriétés de votre carte réseau et sélectionnez <code className="action">Protocole Internet version 6 (TCP/IPv6)</code> > <code className="action">Propriétés</code>.
2. Saisissez l'adresse IPv6, la longueur du préfixe et la passerelle IPv6 par défaut.

Pour les détails d'adressage et la configuration de la passerelle propres à OVHcloud, consultez le guide [Configuration de l'IPv6](/guides/bare-metal-cloud/dedicated-servers/network-ipv6).
#### 4d — Backup Storage

Le Backup Storage est un espace de stockage inclus avec votre serveur dédié. Il est accessible via les protocoles FTP, CIFS ou NFS et convient particulièrement comme sauvegarde complémentaire.

**Dans l'espace client OVHcloud**

1. Dans l'onglet <code className="action">Matériel</code> de votre serveur, cliquez sur <code className="action">Activer le backup storage</code>. Une fois actif, l'onglet affiche l'adresse du serveur FTP et l'identifiant, et vous permet de définir le mot de passe de l'espace.
2. Autorisez l'adresse IP de votre serveur à accéder à l'espace.

**Dans Windows Server**

Accédez à l'espace en utilisant l'un des protocoles pris en charge :

- **CIFS** — montez-le en tant que lecteur réseau (<code className="action">Ce PC</code> > <code className="action">Connecter un lecteur réseau</code>).
- **FTP** — connectez-vous avec n'importe quel client FTP, à l'aide des informations de connexion de l'espace client et du mot de passe que vous avez défini.

Pour la procédure d'activation et les protocoles d'accès, consultez le guide [Backup Storage sur les serveurs dédiés](/guides/bare-metal-cloud/dedicated-servers/services-backup-storage) et la page [Backup Storage](/links/bare-metal/backup-storage).

### Étape 5 — Vérifier la configuration <a name="verify"></a>

Vérifiez que chaque service configuré fonctionne comme prévu.

| Vérification | Comment | Résultat attendu |
|---|---|---|
| Accès RDP | Connectez-vous avec <code className="action">Connexion Bureau à distance</code> sur l'IPv4 principale | Le bureau Windows s'affiche |
| Additional IP | Depuis un hôte externe, exécutez `ping` vers l'Additional IP | Le serveur répond |
| vRack | Depuis un autre membre du vRack, exécutez `ping` vers l'IP privée | Le serveur répond |
| IPv6 | Exécutez `ping -6 ipv6.google.com` depuis le serveur | Vous recevez des réponses |
| Backup Storage | Ouvrez le lecteur CIFS monté ou connectez-vous en FTP | Vous pouvez lire et écrire des fichiers |

### Dépannage <a name="troubleshooting"></a>

| Problème | Cause possible | Solution |
|---|---|---|
| La connexion RDP échoue | Règles de pare-feu ou restrictions réseau sur l'appareil local | Vérifiez les paramètres du pare-feu et du réseau locaux ; connectez-vous via la [console IPMI](/guides/bare-metal-cloud/dedicated-servers/ipmi) en secours. |
| Additional IP injoignable | Masque ou passerelle incorrects dans Windows | Vérifiez à nouveau les valeurs saisies dans les paramètres TCP/IPv4 avancés. |
| Membre du vRack injoignable | Mauvais sous-réseau privé ou passerelle définie sur la carte privée | Confirmez le plan d'adressage privé et laissez la passerelle vide. |
| Serveur hors ligne après une modification | Erreur de configuration lors de la mise en place | Démarrez en [mode rescue](/guides/bare-metal-cloud/dedicated-servers/rescue-customer-windows) pour inspecter et réparer la configuration. |

## Aller plus loin

- [Activer le pare-feu sur un serveur Windows](/guides/bare-metal-cloud/dedicated-servers/activate-port-firewall-soft-win)
- [Configurer un RAID logiciel sur un serveur Windows](/guides/bare-metal-cloud/dedicated-servers/raid-soft-windows)
- [Utiliser le mode rescue sur un serveur Windows](/guides/bare-metal-cloud/dedicated-servers/rescue-customer-windows)
- [Utiliser la console IPMI avec les serveurs dédiés](/guides/bare-metal-cloud/dedicated-servers/ipmi)
- [Gérer vos licences OVHcloud](/guides/bare-metal-cloud/dedicated-servers/windows-key)
- [API OVHcloud et installation du système d'exploitation](/guides/bare-metal-cloud/dedicated-servers/api-os-installation)

Échangez avec notre [communauté d'utilisateurs](https://community.ovh.com/).
