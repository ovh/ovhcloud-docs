---
title: Erste Schritte mit Valkey auf Web Cloud Databases
description: Erfahren Sie, wie Sie Ihre IP-Adressen autorisieren, die Verbindungsparameter Ihrer Valkey-Instanz abrufen und diese über die Befehlszeile testen
lastUpdated: 2026-09-17
---

import { Tab, Tabs } from '@rspress/core/theme';

## Ziel

**Valkey** ist ein quelloffener **In-Memory**-Datenspeicher, der mit den Clients und Protokollen des Redis Ökosystems (RESP, REdis Serialization Protocol) kompatibel ist. Er eignet sich für **Caches**, **Sessions**, **leichtgewichtige Warteschlangen** und **Zähler** mit hoher Performance.

**Erfahren Sie, wie Sie sich mit Ihrer Valkey-Instanz auf Web Cloud Databases verbinden: Autorisierung der IP-Adressen, Abruf der Verbindungsparameter und Test über einen Befehlszeilen-Client.**

:::warning
Sichern Sie den Zugriff auf Ihre Instanz ab (autorisierte IPs, sichere Passwörter) und testen Sie diesen in der Vorproduktion, bevor Sie live gehen.

:::

[[fragment:support-scope]]

## Voraussetzungen

- Sie verfügen über eine [Valkey-Instanz auf Web Cloud Databases](/links/web/databases) (in einem [Webhosting](/links/web/hosting) der Pakete Performance, Agency, Agency Plus oder Agency Max enthalten).

[[cpnav:web-cloud-databases]]

## In der praktischen Anwendung

### Teil 1 — Die Zugriffs-IP-Adresse autorisieren

Standardmäßig verweigert Ihre Valkey-Instanz öffentliche Verbindungen. Um die IP-Adresse zu autorisieren (auf die Whitelist zu setzen), von der aus Sie sich verbinden, lesen Sie unsere Anleitung "[Web Cloud Databases - Wie autorisiere ich eine IP-Adresse?](/guides/web-cloud/databases/db-authorise-ip-mask)"

Wiederholen Sie den Vorgang für jeden Rechner, der auf Ihre Valkey-Instanz zugreifen soll.

### Teil 2 — Die Verbindungsparameter vorbereiten <a id="get-credentials"></a>

#### Die Verbindungsparameter abrufen

Klicken Sie auf die unten stehenden Tabs, um die **2** Schritte nacheinander anzuzeigen.

<Tabs>
  <Tab label="Schritt 1">
    Wählen Sie in der Liste Ihrer Dienste die betreffende Lösung aus.

    <img className="thumbnail" alt="Liste der Web Cloud Databases Dienste im OVHcloud Kundencenter" src="/images/assets/screens/control-panel/product-selection/web-cloud/web-cloud-databases.png" loading="lazy" />
  </Tab>
  <Tab label="Schritt 2">
    Notieren Sie sich in der Registerkarte <code className="action">Allgemeine Informationen</code> die folgenden Angaben:

    - **Hostname**: zum Beispiel `aaXXXXX-XXX.eu.clouddb.ovh.net`.
    - **Port**: zum Beispiel `6379`.
  </Tab>
</Tabs>

:::info
OVHcloud bietet auf den Redis- und Valkey-Datenbankservern keinen über TLS verschlüsselten Port an. Die Kommunikation muss ausschließlich über den unverschlüsselten Standardport erfolgen.

:::

#### Das Passwort des Benutzers `default` festlegen

Ihre Valkey-Instanz wird mit einem Benutzer `default` geliefert, dem einzigen verfügbaren Konto: Verwenden Sie es, um sich zu verbinden.

:::warning
Sein Passwort wird im OVHcloud Kundencenter niemals angezeigt. Sie müssen es mindestens einmal ändern, um es zu erfahren.

:::

Klicken Sie auf die unten stehenden Tabs, um die **4** Schritte nacheinander anzuzeigen.

<Tabs>
  <Tab label="Schritt 1">
    Wählen Sie in der Liste Ihrer Dienste die betreffende Lösung aus.

    <img className="thumbnail" alt="Liste der Web Cloud Databases Dienste im OVHcloud Kundencenter" src="/images/assets/screens/control-panel/product-selection/web-cloud/web-cloud-databases.png" loading="lazy" />
  </Tab>
  <Tab label="Schritt 2">
    Klicken Sie auf der angezeigten Seite auf die Registerkarte <code className="action">Benutzer und Rechte</code>.

    <img className="thumbnail" alt="Registerkarte Benutzer und Rechte einer Web Cloud Databases Instanz" src="/images/assets/screens/control-panel/product-selection/web-cloud/web-cloud-databases/users-and-rights.png" loading="lazy" />
  </Tab>
  <Tab label="Schritt 3">
    Klicken Sie auf die Schaltfläche <code className="action">...</code> rechts neben dem Benutzer `default` und anschließend auf <code className="action">Passwort ändern</code>.

    <img className="thumbnail" alt="Menü Passwort ändern rechts neben einem Benutzer in der Registerkarte Benutzer und Rechte" src="/images/assets/screens/control-panel/product-selection/web-cloud/web-cloud-databases/users-and-rights/wcdb-change-password.png" loading="lazy" />
  </Tab>
  <Tab label="Schritt 4">
    Geben Sie ein sicheres Passwort ein, bestätigen Sie dieses und klicken Sie anschließend auf <code className="action">Bestätigen</code>. Warten Sie einige Minuten, bis die Änderung wirksam wird, bevor Sie die Verbindung testen.

    <img className="thumbnail" alt="Fenster zur Passwortänderung mit den einzuhaltenden Kriterien" src="/images/assets/screens/control-panel/product-selection/web-cloud/web-cloud-databases/users-and-rights/wcdb-change-password-window.png" loading="lazy" />
  </Tab>
</Tabs>

#### Ihre Angaben notieren

Bewahren Sie die folgenden Angaben auf:

- Hostname
- Port
- Passwort des Benutzers `default`

Diese Angaben sind erforderlich, um die Verbindung zu Ihrer Instanz über einen Valkey-Client oder Ihre Anwendung zu testen.

### Teil 3 — Die Verbindung mit einem Befehlszeilen-Client testen

Um zu überprüfen, ob Ihre Valkey-Instanz funktionsfähig ist, verbinden Sie sich ein erstes Mal mit ihr:

#### Den Verbindungs-Client installieren

In diesem Beispiel verwenden wir den Client `redis-cli`, der mit Valkey kompatibel und einfach zu installieren ist. Sie können den Client Ihrer Wahl verwenden. Öffnen Sie ein Terminal und führen Sie je nach Betriebssystem den folgenden Befehl aus.

<Tabs>
  <Tab label="Debian/Ubuntu">
    ```bash
    sudo apt update && sudo apt -y install redis-tools
    ```
  </Tab>
  <Tab label="AlmaLinux/Rocky">
    ```bash
    sudo dnf -y install redis
    ```
  </Tab>
  <Tab label="macOS">
    Installieren Sie [Homebrew](https://brew.sh/de/), indem Sie den Anweisungen auf der zugehörigen Website folgen, und installieren Sie anschließend `redis-cli`:

    ```bash
    brew install redis
    ```
  </Tab>
  <Tab label="Windows">
    Geben Sie im Startmenü `PowerShell` ein, klicken Sie mit der rechten Maustaste auf das Ergebnis und anschließend auf <code className="action">Als Administrator ausführen</code>.

    Installieren Sie [Chocolatey](https://chocolatey.org/install), indem Sie den folgenden Befehl ausführen:

    ```powershell
    Set-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))
    ```

    Installieren Sie `redis-cli`:

    ```powershell
    choco install redis-64 -y
    ```
  </Tab>
</Tabs>

Überprüfen Sie nach der Installation des Clients dessen Funktionsfähigkeit, indem Sie den folgenden Befehl ausführen:

```bash
redis-cli --version
```

#### Sich mit der Instanz verbinden

Führen Sie in Ihrem Terminal den folgenden Befehl aus:

```bash
redis-cli -h <hostname> -p 6379
```

Ersetzen Sie `<hostname>` durch Ihren Hostnamen und `6379` durch Ihren Port, falls dieser abweicht. Um diese Werte abzurufen, lesen Sie den Abschnitt "[Teil 2 — Die Verbindungsparameter vorbereiten](#get-credentials)".

#### Überprüfen, ob alles funktioniert

Wenn die Verbindung erfolgreich ist, wird die Eingabeaufforderung Ihrer Instanz angezeigt. Authentifizieren Sie sich mit dem Befehl `AUTH` und ersetzen Sie dabei `<password>` durch das Passwort des Benutzers `default`:

```console
aaXXXXX-XXX.eu.clouddb.ovh.net:6379> AUTH <password>
OK
```

Überprüfen Sie anschließend, ob der Server antwortet:

```console
aaXXXXX-XXX.eu.clouddb.ovh.net:6379> PING
```

Die Datenbank muss antworten:

```console
PONG
```

Versuchen Sie auch Folgendes:

```console
aaXXXXX-XXX.eu.clouddb.ovh.net:6379> SET ovh-test-connexion "hello"
aaXXXXX-XXX.eu.clouddb.ovh.net:6379> GET ovh-test-connexion
```

Sie müssen Folgendes sehen:

```console
"hello"
```

Löschen Sie den Testschlüssel:

```console
aaXXXXX-XXX.eu.clouddb.ovh.net:6379> DEL ovh-test-connexion
```

Geben Sie zum Beenden Folgendes ein:

```console
aaXXXXX-XXX.eu.clouddb.ovh.net:6379> QUIT
```

Wenn die vorherigen Tests funktionieren, ist Ihre Valkey-Instanz betriebsbereit.

## Weiterführende Informationen

Kontaktieren Sie für spezialisierte Dienstleistungen (SEO, Web-Entwicklung etc.) die [OVHcloud Partner](/links/partner).

Wenn Sie Hilfe bei der Nutzung und Konfiguration Ihrer OVHcloud Lösungen benötigen, beachten Sie unsere [Support-Angebote](/links/support).

Treten Sie unserer [User Community](/links/community) bei.
