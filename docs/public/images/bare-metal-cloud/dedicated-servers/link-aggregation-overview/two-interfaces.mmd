flowchart TB
    subgraph DEFAULT["Default"]
        DPUB["public<br/>(public IP)"]
        DPRIV["private<br/>(vRack)"]
    end

    subgraph OLA["OLA"]
        subgraph OBOND["bond (vRack)"]
            OPUB["public"]
            OPRIV["private"]
        end
    end

    %% Invisible link only: it constrains the layout and carries no meaning.
    %% Stack the two modes vertically rather than side by side, to match
    %% four-interfaces.mmd and keep both diagrams on the same visual grid.
    DEFAULT ~~~ OLA
