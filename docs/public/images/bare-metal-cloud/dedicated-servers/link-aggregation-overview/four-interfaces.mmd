flowchart TB
    subgraph DEFAULT["Default"]
        subgraph DPUBBOND["bond (public IP)"]
            DPUB1["public 1"]
            DPUB2["public 2"]
        end
        subgraph DPRIVBOND["bond (vRack)"]
            DPRIV1["private 1"]
            DPRIV2["private 2"]
        end
    end

    subgraph OLA["OLA"]
        subgraph OBOND["bond (vRack)"]
            OPUB1["public 1"]
            OPUB2["public 2"]
            OPRIV1["private 1"]
            OPRIV2["private 2"]
        end
    end

    %% Invisible links only: they constrain the layout and carry no meaning.
    %% Stack the two modes vertically rather than side by side, so the diagram
    %% fits the content column and its labels stay readable at 1:1.
    DPUBBOND ~~~ DPRIVBOND
    DEFAULT ~~~ OLA
    %% Lay the four aggregated interfaces out as a 2x2 grid, mirroring the
    %% public/private rows of the default mode.
    OPUB1 ~~~ OPRIV1
    OPUB2 ~~~ OPRIV2
